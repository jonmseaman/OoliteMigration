# Selectors the game sends by name that tools/check-selector-types.py cannot find by itself
# (ADR-0043 Amendment 3, item 21): NSSelectorFromString on a built string, a SEL stored in data.
# One selector per line, with or without a leading "-"; "#" starts a comment.
