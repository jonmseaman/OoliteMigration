PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -m pytest tools/test_oxp_corpus.py -q
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c "import sys;sys.path.insert(0,'tools');import oxp_corpus as o;from pathlib import Path;m=o.load_manifest(Path('tools/oxp-corpus/manifest.json'));c=o.load_catalog();p=o.check_manifest_schema(m);e=m['entries'];miss=set(c)-set(e);ok=[x for x in e.values() if x.get('status')=='ok'];bad=[u for u,x in e.items() if x.get('status')=='ok' and not (o.SHA256_RE.match(x.get('sha256','')) and isinstance(x.get('size'),int))];assert len(c)==818,'catalog is %d URLs, expected 818'%len(c);assert not p,'manifest schema problems: %s'%p[:3];assert not miss,'%d catalog URLs absent from manifest e.g. %s'%(len(miss),sorted(miss)[:2]);assert not bad,'%d ok entries lack a well-formed sha256/size e.g. %s'%(len(bad),bad[:2]);assert len(ok)>=700,'only %d of 818 entries fetched ok'%len(ok);print('MANIFEST OK: 818 catalog URLs, %d entries, %d ok, all with url+size+sha256'%(len(e),len(ok)))"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c "import sys;sys.path.insert(0,'tools');import oxp_corpus as o;from pathlib import Path;F=Path('tools/oxp-corpus/fixtures');m=o.load_manifest(F/'manifest.json');C=F/'cache';g='https://example.invalid/fixtures/Good_Expansion_1.0.oxz';b='https://example.invalid/fixtures/Corrupt_Expansion_1.0.oxz';ge=m['entries'][g];be=m['entries'][b];r=o.verify_entry(ge,C);assert r[0],'the intact fixture entry failed verification: %s'%r[1];assert o.blob_path(C,b).stat().st_size==be['size'],'the corrupt fixture no longer has the manifest size, so a size check alone would catch it and this proves nothing about checksums';r2=o.verify_entry(be,C);assert not r2[0],'THE VERIFIER ACCEPTED A CORRUPTED CACHE BLOB (same size, different bytes)';assert 'sha256' in r2[1] and 'CORRUPT' in r2[1],'corruption detected but not reported as a sha256 mismatch: %s'%r2[1];print('CHECKSUM VERIFY OK: intact accepted, same-size corruption rejected -> %s'%r2[1])"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c "import sys,json,shutil,tempfile;sys.path.insert(0,'tools');import oxp_corpus as o;from pathlib import Path;F=Path('tools/oxp-corpus/fixtures');g='https://example.invalid/fixtures/Good_Expansion_1.0.oxz';t=Path(tempfile.mkdtemp());C=t/'cache';shutil.copytree(F/'cache',C);m=json.loads((F/'manifest.json').read_text());m['entries']={g:m['entries'][g]};net=o.RecordingOpener();s=o.fetch_all([g],C,m,opener=net,rate_limit=0);assert net.attempts==[],'A SECOND RUN OVER A POPULATED CACHE TOUCHED THE NETWORK: %s'%net.attempts;assert (s.downloaded,s.failed,s.skipped)==(0,0,1),'second run stats were %s, expected downloaded=0 failed=0 skipped=1'%s;o.blob_path(C,g).unlink();s2=o.fetch_all([g],C,m,opener=net,rate_limit=0);assert net.attempts==[g],'the fetcher SKIPPED an entry whose cached blob is gone, so the skip is unconditional and the no-op claim above is vacuous: attempts=%s'%net.attempts;shutil.rmtree(t,ignore_errors=True);print('IDEMPOTENCE OK: populated cache -> 0 network attempts; missing blob -> 1 attempt')"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); git ls-files -z tools/oxp-corpus | "$PY" -c "import sys,os;f=[x.decode() for x in sys.stdin.buffer.read().split(b'\0') if x];big=[(p,os.path.getsize(p)) for p in f if os.path.exists(p) and os.path.getsize(p)>3*1024*1024];assert not big,'large blobs committed under tools/oxp-corpus (the byte cache belongs OUTSIDE the repo): %s'%big;assert 'tools/oxp-corpus/manifest.json' in f,'no manifest.json is committed under tools/oxp-corpus';print('REPO HYGIENE OK: %d tracked files under tools/oxp-corpus, none over 3 MiB'%len(f))"
