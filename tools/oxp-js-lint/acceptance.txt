PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); NODE=$(command -v node || echo "C:/Users/jon/scoop/apps/nodejs/current/node"); "$NODE" tools/oxp-js-lint/lint.js rules | "$PY" -c "import sys,json;d=json.load(sys.stdin);want=['catch-if','e4x','quote-method','to-source','uneval','let-block','legacy-accessor','expression-closure','legacy-generator'];miss=[r for r in want if r not in d['enabled']];assert not miss,'RULES NOT ENABLED in tools/oxp-js-lint/eslint.config.js: %s (enabled=%s)'%(miss,d['enabled']);assert sorted(d['all'])==sorted(want),'rules.js implements %s, expected the 8 named in bead oo-ctq plus legacy-generator (oo-1gc.16)'%sorted(d['all']);print('RULE SET OK: all 9 Mozilla-only detectors implemented and enabled')"
NODE=$(command -v node || echo "C:/Users/jon/scoop/apps/nodejs/current/node"); PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$NODE" tools/oxp-js-lint/lint.js scan tools/oxp-js-lint/fixtures --json --allow-hits | "$PY" -c "import sys,json;d=json.load(sys.stdin);byf={r['file'].split('/')[-1]:r['findings'] for r in d['results']};want={'catch-if.js':'catch-if','e4x.js':'e4x','quote-method.js':'quote-method','to-source.js':'to-source','uneval.js':'uneval','let-block.js':'let-block','legacy-accessor.js':'legacy-accessor','expression-closure.js':'expression-closure','legacy-generator.js':'legacy-generator'};miss=[(f,r) for f,r in want.items() if f not in byf];assert not miss,'fixture files absent from the scan: %s (saw %s)'%(miss,sorted(byf));bad=[(f,r,sorted({x['rule'] for x in byf[f]})) for f,r in want.items() if r not in {x['rule'] for x in byf[f]}];assert not bad,'A MOZILLA-ONLY CONSTRUCT WAS NOT FLAGGED - fixture/expected-rule/actual-rules: %s'%bad;print('POSITIVE CONTROL OK: each of the 9 fixtures flagged by its own rule (%d findings over %d files)'%(d['findings'],d['files']))"
NODE=$(command -v node || echo "C:/Users/jon/scoop/apps/nodejs/current/node"); PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$NODE" tools/oxp-js-lint/lint.js scan tools/oxp-js-lint/fixtures/clean.js --json --allow-hits | "$PY" -c "import sys,json;d=json.load(sys.stdin);assert d['files']==1,'expected to scan exactly the clean fixture, scanned %d files'%d['files'];f=d['results'][0]['findings'];assert not f,'FALSE POSITIVES on portable ES5 code (the clean fixture mentions every banned construct only inside strings, comments and a regex): %s'%[(x['rule'],x['line'],x['snippet']) for x in f];print('NEGATIVE CONTROL OK: clean ES5 fixture with near-miss strings/comments/regex -> 0 findings')"
NODE=$(command -v node || echo "C:/Users/jon/scoop/apps/nodejs/current/node"); PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c "import os,sys;d='upstream/oolite/Resources/Scripts';assert os.path.isdir(d),'the in-tree script directory %s does not exist'%d;n=len([f for f in os.listdir(d) if f.lower().endswith('.js')]);assert n==31,'EXPECTED 31 in-tree scripts in %s, found %d'%(d,n);print('31 in-tree scripts present')" && "$NODE" tools/oxp-js-lint/lint.js scan upstream/oolite/Resources/Scripts --json | "$PY" -c "import sys,json;d=json.load(sys.stdin);assert d['files']==31,'scanned %d files, expected the 31 in-tree scripts'%d['files'];assert len(d['rules'])==9,'only %d rules were live during the zero-hit scan, so zero hits proves nothing: %s'%(len(d['rules']),d['rules']);hits=[(r['file'],x['rule'],x['line']) for r in d['results'] for x in r['findings']];assert not hits,'MOZILLA-ONLY JS FOUND IN THE 31 IN-TREE SCRIPTS: %s'%hits[:5];print('IN-TREE OK: 31 scripts, all 9 rules live, 0 hits')"
NODE=$(command -v node || echo "C:/Users/jon/scoop/apps/nodejs/current/node"); PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); OUT=$("$PY" -c "import os;b=(os.environ.get('LOCALAPPDATA') or os.path.expanduser('~')).replace(chr(92),'/')+'/Temp';os.makedirs(b,exist_ok=True);print(b+'/oxp-js-lint-accept-report.json')") && "$NODE" tools/oxp-js-lint/lint.js corpus --out "$OUT" --limit 60 && "$PY" -c "import json,sys;p=sys.argv[1];d=json.load(open(p));t=d['totals'];assert len(d['rules'])==9,'corpus scan ran with %d rules: %s'%(len(d['rules']),d['rules']);assert t['expansions']==60,'report covers %d expansions, expected the 60 requested'%t['expansions'];assert t['scanned']>=40,'only %d of 60 cached expansions could be read (cache at %s) - the per-expansion report is not evidence of a corpus scan'%(t['scanned'],d['cache_dir']);assert t['js_files']>=40,'only %d .js members were extracted from %d expansions; a scan that opens no JavaScript cannot flag any'%(t['js_files'],t['scanned']);assert len(d['expansions'])==t['expansions'],'per-expansion report has %d records for %d expansions'%(len(d['expansions']),t['expansions']);missing=[e for e in d['expansions'] if 'url' not in e or 'status' not in e or 'findings' not in e];assert not missing,'%d report records lack url/status/findings'%len(missing);print('CORPUS REPORT OK: %d expansions, %d read from the cache, %d .js members, %d findings, per-expansion records=%d'%(t['expansions'],t['scanned'],t['js_files'],t['findings'],len(d['expansions'])))" "$OUT"
