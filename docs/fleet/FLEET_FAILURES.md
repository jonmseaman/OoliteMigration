# Fleet failures

The research log ([I4](../infra/4-metrics.md)): every bead the fleet could not finish and handed
to the frontier tier. Appended by `scripts/escalate.sh`; a human adds the "what it needed" column
when the bead is eventually done. This is the document that answers the question the project was
started to answer: how far did AI get, and where did it stop.

| Date | Bead | Phase | Attempts | Reason | What it needed (filled in later) |
|---|---|---|---|---|---|
| 2026-09-19 | oo-phi | 0 | 0 | claude: oo-phi is a frontier-labeled seam that a fleet worker should never have been dispatched; its dependency on the oo-do10 Phase 0 review is correct ordering (the gate must not check boxes against evidence the review has not yet re-verified), review beads already do not block fleet drain per goal-check.sh, and oo-phi's acceptance is a placeholder 'exit 1' that only a frontier agent may replace. | |
| 2026-09-19 | oo-hq8 | 0 | 0 | claude: The dependency on oo-e7c is correct and in fact insufficient: oo-hq8's definition of done requires a generated Phase 1 js-retarget bead to be closed by accept.sh, which cannot exist until the JSEngine façade (1.1), its exemplar retarget (1.1x) and the refactor scripts (1.2) land, so it cannot be rescoped into Phase 0 — and it is a frontier bead with a non-executable acceptance block that no fleet worker should hold. | |
| 2026-09-19 | oo-1sk | 1 | 0 | claude: The file has zero JS_* call sites so the bead is vacuous by construction of a wrong selection rule in the generator, and its tier-b acceptance line is independently red on main from a non-transitively-closed DLL set in a repo test that a worker is forbidden to touch.. Partial work: branch bead/oo-1sk, 1 commit(s) ahead of main (worktree.sh oo-1sk recreates the checkout) | |
| 2026-09-19 | oo-0s4 | 1 | 0 | claude: The bead is vacuous (zero JS_* call sites; the file uses only OOJS* wrappers) because gen-stories.py selects on an include rather than on the acceptance predicate, and its second acceptance line is pre-existing-red on main because tier-a.sh applies clang-tidy absolutely rather than baseline-relative — two fleet-wide defects a cheap worker cannot fix without breaking hard rules 3 or 9.. Partial work: branch bead/oo-0s4, 0 commit(s) ahead of main (worktree.sh oo-0s4 recreates the checkout) | |
| 2026-09-19 | oo-30t | 1 | 0 | orchestrator: file has zero JS_* call sites (grep -c confirms 0), duplicate root cause of already-tracked generator-bug oo-utqt (tools/gen-stories.py:374 selector predicate mismatch). Not filing another generator-bug bead -- oo-utqt/oo-hj97 already cover it.. Partial work: branch bead/oo-30t, 0 commit(s) ahead of main (worktree.sh oo-30t recreates the checkout) | |
| 2026-09-19 | oo-44p | 1 | 0 | orchestrator: file has zero JS_* call sites (grep -c confirms 0), duplicate root cause of already-tracked generator-bug oo-utqt (tools/gen-stories.py:374 selector predicate mismatch). Not filing another generator-bug bead -- oo-utqt/oo-hj97 already cover it.. Partial work: branch bead/oo-44p, 0 commit(s) ahead of main (worktree.sh oo-44p recreates the checkout) | |
| 2026-09-19 | oo-2ol | 1 | 0 | claude: The task and the worker's implementation are both correct and match the exemplar; the blocker is a documented low-similarity limitation (and a wrong escape-hatch instruction) in tools/guardrails.sh that no edit to OOJSWaypoint.mm can clear and that a cheap model must not fix, and it blocks every small file in the js-retarget sweep.. Partial work: branch bead/oo-2ol, 3 commit(s) ahead of main (worktree.sh oo-2ol recreates the checkout) | |
| 2026-09-19 | oo-45g | 1 | 6 | claude: Attempt 5 was green on every stage and still exited 1 on 'GREEN but OVER BUDGET: 914s > 600s', because tier-b.sh:615 runs the whole 8-scenario component suite (769-957s, alone over the 600s budget) instead of the 'pytest -m fast' subset seam 0.9b specifies and no 'fast' marker exists in tests/component, so the stored acceptance line is structurally unpassable for every bead in the sweep and needs a gate-policy fix plus ADR, not another retry.. Partial work: branch bead/oo-45g, 1 commit(s) ahead of main (worktree.sh oo-45g recreates the checkout) | |
| 2026-09-20 | oo-sjvz | 1 | 0 | claude: Pinning ship keys or setting bounty requires a new step definition or a rewrite of _spawn in steps/world_steps.py, which ADR-0018 §5 and the step library's own header classify as a new interface (sizing check 5) and therefore seam 0.13 frontier work, not fleet work; independently, guardrails' feature_units() keys live test units on exact step-line text (tools/guardrails.sh:695-714, design note line 656), so every edit to an existing step's wording is refused by construction, closing the only in-scope path.. Partial work: branch bead/oo-sjvz, 2 commit(s) ahead of main (worktree.sh oo-sjvz recreates the checkout) | |
