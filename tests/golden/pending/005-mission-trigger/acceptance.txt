PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); for f in tests/golden/mission_trigger.py tests/golden/check_mission_trigger_evidence.py tests/golden/test_mission_trigger.py; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; S=$( { ls goldens/windows-x64/005-mission-trigger/spec.json tests/golden/pending/005-mission-trigger/spec.json 2>/dev/null || true; } | head -1 ); G=$( { ls goldens/windows-x64/005-mission-trigger/state.json tests/golden/pending/005-mission-trigger/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/005-mission-trigger/provenance.json tests/golden/pending/005-mission-trigger/provenance.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/005-mission-trigger/frame.grid tests/golden/pending/005-mission-trigger/frame.grid 2>/dev/null || true; } | head -1 ); for f in "$S" "$G" "$P" "$F"; do test -n "$f" && test -f "$f" || { echo "MISSING: scenario 005 spec/golden/provenance/frame.grid (the guarded goldens/ path is searched FIRST, then tests/golden/pending/, so landing is a pure git mv)"; exit 1; }; done; test -f upstream/oolite-tests/Checklist-files/Missions/CloakingDevice.oolite-save || { echo "MISSING: the CloakingDevice checklist save this scenario loads"; exit 1; }; test "$(wc -c < "$F")" -eq 4096 || { echo "FAIL: $F is $(wc -c < "$F") bytes, not the 4096-byte 64x64 luminance grid"; exit 1; }; "$PY" -m py_compile tests/golden/mission_trigger.py tests/golden/check_mission_trigger_evidence.py tests/golden/test_mission_trigger.py && echo "PASS: scenario 005-mission-trigger script, evidence checker, tests, spec, golden, provenance, frame grid and the .oolite-save fixture are all present and importable (spec=$S golden=$G prov=$P frame=$F)"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c 'import json,os,sys;S=[q for q in ("goldens/windows-x64/005-mission-trigger/spec.json","tests/golden/pending/005-mission-trigger/spec.json") if os.path.isfile(q)][0];P=[q for q in ("goldens/windows-x64/005-mission-trigger/provenance.json","tests/golden/pending/005-mission-trigger/provenance.json") if os.path.isfile(q)][0];s=json.load(open(S,encoding="utf-8"));p=json.load(open(P,encoding="utf-8"));req=["seed","system_id","ticks","tick_seconds","quant_decimals","load_save","control_save","mission_script_name","mission_variable_key","mission_variable_key_in_file","expected_counter_in_file","expected_counter_in_engine","expected_populator_key","ambush_leader_role","expected_ambush_leader_script","expected_ambush_escort_group_count","expected_spawned_roles","expected_live_mission_handlers","expected_mission_variables","expected_commander_name","expected_galaxy_number"];miss=[k for k in req if k not in s];sys.exit("FAIL: spec.json does not pin %s; without them the scenario is not reproducible"%miss) if miss else None;sys.exit("FAIL: spec quant_decimals=%r but the storage policy is 3"%s["quant_decimals"]) if s["quant_decimals"]!=3 else None;sys.exit("FAIL: spec ticks=%r, must be a positive int"%s["ticks"]) if not isinstance(s["ticks"],int) or s["ticks"]<1 else None;sys.exit("FAIL: spec tick_seconds=%r, must be a positive number"%s["tick_seconds"]) if not isinstance(s["tick_seconds"],(int,float)) or s["tick_seconds"]<=0 else None;sys.exit("FAIL: spec load_save=%r, expected the CloakingDevice checklist save"%s["load_save"]) if not s["load_save"].endswith("Missions/CloakingDevice.oolite-save") else None;sys.exit("FAIL: spec control_save=%r is the same file as load_save; a differential whose two arms are the same file cannot move"%s["control_save"]) if s["control_save"]==s["load_save"] else None;sys.exit("FAIL: spec pins file=%r engine=%r. This scenario asserts a MISSION SCRIPT INCREMENTED the counter; any other relation is a spec that no longer describes that experiment."%(s["expected_counter_in_file"],s["expected_counter_in_engine"])) if s["expected_counter_in_engine"]!=s["expected_counter_in_file"]+1 else None;sys.exit("FAIL: spec expected_counter_in_file=%r but the fixture pins 6, and that EXACT value is why the incremented counter crosses the mission script > 6 threshold at oolite-cloaking-device-mission.js:66"%s["expected_counter_in_file"]) if s["expected_counter_in_file"]!=6 else None;sys.exit("FAIL: spec expected_galaxy_number=%r but the trigger guard is galaxyNumber === 4; in any other galaxy the handler returns without doing anything"%s["expected_galaxy_number"]) if s["expected_galaxy_number"]!=4 else None;sys.exit("FAIL: spec expected_mission_variables[%r]=%r but the post-trigger engine value is %r; a post-trigger set still holding the FILE value would silently turn this scenario back into a deserialiser test"%(s["mission_variable_key"],s["expected_mission_variables"].get(s["mission_variable_key"]),s["expected_counter_in_engine"])) if s["expected_mission_variables"].get(s["mission_variable_key"])!=s["expected_counter_in_engine"] else None;sys.exit("FAIL: spec expected_spawned_roles=%r, not the measured set. asp-pirate is deliberately NOT counted: shipdata.plist gives the ordinary Asp that role, the ambient populator writes to it (measured 2, 3 and 4 across ten runs) and counting it aborted 2 of 10 runs."%s["expected_spawned_roles"]) if s["expected_spawned_roles"]!={"asp-cloaked":1} else None;sys.exit("FAIL: spec expected_live_mission_handlers=%r, not the measured two"%s["expected_live_mission_handlers"]) if sorted(s["expected_live_mission_handlers"])!=["startUp","systemWillPopulate"] else None;src=open("tests/golden/mission_trigger.py",encoding="utf-8").read();[sys.exit("FAIL: mission_trigger.py never reads spec[%r]; the knob is decoration - if someone changed it, no line would go red"%k) for k in req if k!="quant_decimals" and "spec["+chr(34)+k+chr(34)+"]" not in src];kn=p.get("scenario_knobs");sys.exit("FAIL: %s records no scenario_knobs; the seed/system/ticks the golden was BLESSED with are unrecorded, so nothing can detect the spec drifting away from them (bead oo-3ya)"%P) if not kn else None;[sys.exit("FAIL: provenance scenario_knobs does not record %r"%k) for k in ("seed","system_id","ticks","tick_seconds","quant_decimals") if k not in kn];drift={k:(kn[k],s.get(k)) for k in kn if s.get(k)!=kn[k]};sys.exit("FAIL: spec.json disagrees with the knobs this golden was blessed with (%s records blessed-vs-spec %r). A fresh-run-vs-golden comparison CANNOT catch this, because changing a knob changes BOTH sides. Re-bless deliberately or restore the spec - do NOT edit one side to match."%(P,drift)) if drift else None;sys.exit("FAIL: provenance quant_decimals=%r but the policy is 3"%p.get("quant_decimals")) if p.get("quant_decimals")!=3 else None;sys.exit("FAIL: provenance dump_tool=%r, expected tests/golden/mission_trigger.py"%p.get("dump_tool")) if p.get("dump_tool")!="tests/golden/mission_trigger.py" else None;print("PASS: seed=%s system_id=%s ticks=%s tick_seconds=%s quant=3 save=%s control=%s counter %s->%s, all %d knob(s) read by mission_trigger.py and all %d equal to the values recorded in the golden provenance"%(s["seed"],s["system_id"],s["ticks"],s["tick_seconds"],s["load_save"].split("/")[-1],s["control_save"].split("/")[-1],s["expected_counter_in_file"],s["expected_counter_in_engine"],len(req),len(kn)))'
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c 'import plistlib,json,os,sys;S=[q for q in ("goldens/windows-x64/005-mission-trigger/spec.json","tests/golden/pending/005-mission-trigger/spec.json") if os.path.isfile(q)][0];s=json.load(open(S,encoding="utf-8"));p=os.path.join(*s["load_save"].split("/"));d=plistlib.load(open(p,"rb"));mv=d.get("mission_variables");sys.exit("FAIL: %s has no mission_variables; the whole point of this scenario is mission state"%p) if not isinstance(mv,dict) else None;k=s["mission_variable_key_in_file"];sys.exit("FAIL: %s no longer carries %r. Without a saved counter there is nothing for the mission script to increment and this scenario core assertion is UNEVALUABLE. If upstream changed the fixture this is a FINDING: re-measure, do NOT edit the assertion to match."%(p,k)) if k not in mv else None;sys.exit("FAIL: %s %s is %r but the spec pins %r, and that EXACT value is why the incremented counter crosses the mission script > 6 threshold"%(p,k,mv.get(k),s["expected_counter_in_file"])) if int(mv[k])!=s["expected_counter_in_file"] else None;sys.exit("FAIL: %s now carries a cloak mission variable. oolite-cloaking-device-mission.js:40 DELETES systemWillPopulate in startUp when cloak is set, so the trigger could NEVER fire and this scenario would be vacuous."%p) if "mission_cloak" in mv else None;sys.exit("FAIL: %s is in galaxy %r but the trigger guard is galaxyNumber === 4; in any other galaxy the handler returns without doing anything"%(p,d.get("galaxy_number"))) if int(d.get("galaxy_number",-1))!=s["expected_galaxy_number"] else None;sys.exit("FAIL: %s player_name=%r, expected %r"%(p,d.get("player_name"),s["expected_commander_name"])) if str(d.get("player_name"))!=s["expected_commander_name"] else None;sys.exit("FAIL: %s written_by_version=%r, expected the %s-era fixture this scenario pins as the save-format compatibility contract"%(p,d.get("written_by_version"),s["save_format_version"])) if str(d.get("written_by_version"))!=s["save_format_version"] else None;c=os.path.join(*s["control_save"].split("/"));cmv=(plistlib.load(open(c,"rb")).get("mission_variables") or {});sys.exit("FAIL: the control save %s ALSO carries %r, so the differential arm cannot show the counter appearing"%(c,k)) if k in cmv else None;print("PASS: the fixture is a %s-era save for commander %r in galaxy %d with %s=%d ON DISK and no cloak variable (so systemWillPopulate is still live), while the control save %s carries no %s at all - the two arms differ in exactly the field this scenario is about"%(d["written_by_version"],d["player_name"],int(d["galaxy_number"]),k,int(mv[k]),os.path.basename(c),k))'
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -m pytest tests/golden/test_mission_trigger.py -q -p no:cacheprovider
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$( { ls goldens/windows-x64/005-mission-trigger/state.json tests/golden/pending/005-mission-trigger/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/005-mission-trigger/provenance.json tests/golden/pending/005-mission-trigger/provenance.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/005-mission-trigger/frame.grid tests/golden/pending/005-mission-trigger/frame.grid 2>/dev/null || true; } | head -1 ); OUT=$("$PY" tests/golden/check_mission_trigger_evidence.py "$G" --label "stored golden 005-mission-trigger" 2>&1); RC=$?; printf "%s\n" "$OUT"; test $RC -eq 0 || { echo "FAIL: the stored golden does not prove a mission script fired from a world-script event; a byte-identical comparison against it would be vacuous"; exit 1; }; "$PY" -c 'import hashlib,json,sys;g,p,f=sys.argv[1],sys.argv[2],sys.argv[3];blob=open(g,"rb").read();prov=json.load(open(p,encoding="utf-8"));arts=prov.get("artifacts") or {};rec=arts.get("state.json");sys.exit("FAIL: %s records no artifacts.state.json digest; without an INDEPENDENT witness, editing the golden changes BOTH sides of every self-comparison and no line can see it (bead oo-gxp)"%p) if not rec else None;sys.exit("FAIL: the stored golden is %d bytes but provenance records %d. The golden and the provenance that witnesses it have diverged; one of them was edited alone. A deliberate re-bless moves BOTH files together and is governed by tools/rebless-approvals.txt."%(len(blob),rec["bytes"])) if len(blob)!=rec["bytes"] else None;got=hashlib.sha256(blob).hexdigest();sys.exit("FAIL: the stored golden hashes to sha256=%s but provenance witnesses sha256=%s. ONE QUANTISED UNIT on one float is enough to do this, and a golden-vs-copy comparison CANNOT catch it because the edit moves both sides. Investigate before re-blessing; do NOT update the digest to match."%(got,rec["sha256"])) if got!=rec["sha256"] else None;frec=arts.get("frame.grid");sys.exit("FAIL: provenance records no artifacts frame.grid entry") if not frec else None;fb=open(f,"rb").read();sys.exit("FAIL: frame.grid is %d bytes but provenance records %d"%(len(fb),frec["bytes"])) if len(fb)!=frec["bytes"] else None;d=json.loads(blob.decode("utf-8"));ev=d.get("evidence") or {};sys.exit("FAIL: the golden carries no top-level mission_variables; the canonical dump itself must carry the mission state") if not isinstance(d.get("mission_variables"),dict) else None;sys.exit("FAIL: the dump top-level mission_variables %r disagrees with evidence.mission_variables %r; one of the two was edited alone"%(d["mission_variables"],ev.get("mission_variables"))) if d["mission_variables"]!=ev.get("mission_variables") else None;sys.exit("FAIL: the blessed golden reports file=%r engine=%r. EQUAL VALUES ARE THE DESERIALISER-COPY SIGNATURE: the save loaded and the world-script event never ran the handler - the exact vacuity this scenario exists to exclude."%(ev.get("mission_counter_in_file"),ev.get("mission_counter_in_engine"))) if ev.get("mission_counter_in_engine")!=ev.get("mission_counter_in_file")+1 else None;txt=blob.decode("utf-8");sys.exit("FAIL: the dump carries a frame digest. llvmpipe is NOT bit-reproducible across runs, so a frame hash inside state.json makes the byte-identical golden comparison flake on renderer noise; frames are compared with the MEASURED tolerance and stored beside the dump as frame.grid.") if ("frame_hash" in txt or "frame_grid" in txt) else None;fc=prov.get("frame_control") or {};sys.path.insert(0,"tests/golden");import frame_hash;tol=frame_hash.derive_tolerance();sys.exit("FAIL: frame_hash.TOLERANCE has moved from the value bead oo-ae9 measured (now %r); re-measure with calibrate.py and re-bless, do NOT adjust it to make a run pass"%tol) if abs(tol-0.004377268476873758)>1e-12 else None;sys.exit("FAIL: provenance frame_control.tolerance=%r is not the measured tolerance %r"%(fc.get("tolerance"),tol)) if abs(float(fc.get("tolerance",0))-tol)>1e-12 else None;sys.exit("FAIL: provenance same_scene_distance=%r is at or beyond the measured tolerance %r; the frame comparison would flake"%(fc.get("same_scene_distance"),tol)) if not float(fc["same_scene_distance"])<tol else None;[sys.exit("FAIL: provenance %s=%r is only %.2fx the measured tolerance; the frame does not discriminate this scenario from that control"%(a,fc[a],float(fc[a])/tol)) for a in ("control_constrictor_distance","control_fresh_game_distance") if float(fc[a])/tol<=2];st=prov.get("stability") or {};sys.exit("FAIL: provenance stability records %r dump(s) in %r distinct digest(s); the bead requires 10 runs producing ONE digest"%(st.get("dumps_written"),st.get("distinct_digests"))) if int(st.get("dumps_written",0))<10 or int(st.get("distinct_digests",0))!=1 else None;print("PASS: the stored golden is byte-for-byte the artifact provenance witnesses (%d bytes, sha256=%s), reports the counter moving %d ON DISK -> %d IN THE ENGINE, and carries mission_variables at top level agreeing with evidence. %d/%d stability runs produced one digest. NOTE THE DELIBERATE ASYMMETRY: frame.grid is recorded in provenance but NOT byte-gated - llvmpipe is not bit-reproducible, so the frame is compared with the measured tolerance %.6f, inside which two launches of this scenario sit (%.6f, %.2fx) while a DIFFERENT checklist save is outside it (%.6f, %.2fx) and a fresh game further still (%.6f, %.2fx)."%(len(blob),got,ev["mission_counter_in_file"],ev["mission_counter_in_engine"],st["identical_dumps"],st["runs"],tol,fc["same_scene_distance"],float(fc["same_scene_distance"])/tol,fc["control_constrictor_distance"],float(fc["control_constrictor_distance"])/tol,fc["control_fresh_game_distance"],float(fc["control_fresh_game_distance"])/tol))' "$G" "$P" "$F"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$( { ls goldens/windows-x64/005-mission-trigger/state.json tests/golden/pending/005-mission-trigger/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/005-mission-trigger/provenance.json tests/golden/pending/005-mission-trigger/provenance.json 2>/dev/null || true; } | head -1 ); "$PY" -c 'import json,sys;g,p=sys.argv[1],sys.argv[2];d=json.load(open(g,encoding="utf-8"));prov=json.load(open(p,encoding="utf-8"));md=prov.get("trigger_differential");sys.exit("FAIL: %s records no trigger_differential; nothing shows the dump can TELL a fired trigger from an unfired one"%p) if not md else None;sub=md.get("subject") or {};ev=d.get("evidence") or {};keys=["commander_name","system_name","system_id","galaxy_number","mission_counter_in_file","mission_counter_in_engine","ambush_populator_registered","ambush_leader","spawned_role_counts","mission_script_loaded"];bad=[k for k in keys if sub.get(k)!=ev.get(k)];sys.exit("FAIL: provenance subject disagrees with the BLESSED golden on %r; the differential was measured against a different dump than the one shipped, so it is evidence about a dump nobody ships"%bad) if bad else None;arms=[];[arms.append((a,md.get(a))) for a in ("control_constrictor","control_fresh_game")];[sys.exit("FAIL: provenance records no %s control arm"%a) for a,v in arms if not v];moved={a:sorted(k for k in keys if v.get(k)!=sub.get(k)) for a,v in arms};[sys.exit("FAIL: control arm %r moves NO field against the subject; the dump does not OBSERVE whether the trigger fired and the golden is vacuous"%a) for a in moved if not moved[a]];[sys.exit("FAIL: control arm %r reports the SAME post-trigger counter as the subject; the counter is then not what distinguishes a fired trigger from an unfired one"%a) for a,v in arms if v.get("mission_counter_in_engine")==sub.get("mission_counter_in_engine")];[sys.exit("FAIL: control arm %r reports the ambush populator REGISTERED; that key is created only by this mission setPopulator call, so a control that has it is not a control"%a) for a,v in arms if v.get("ambush_populator_registered") is not False];[print("  %-20s moves %d/%d trigger field(s): %s"%(a,len(moved[a]),len(keys),", ".join(moved[a]))) for a,v in arms];print("PASS: the blessed dump IS the differential subject and both control arms move the fields this scenario is about - counter %s->%s with the ambush leader %r in the subject, absent in both controls"%(sub["mission_counter_in_file"],sub["mission_counter_in_engine"],(sub.get("ambush_leader") or {}).get("script")))' "$G" "$P"
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; G=$( { ls goldens/windows-x64/005-mission-trigger/state.json tests/golden/pending/005-mission-trigger/state.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/005-mission-trigger/frame.grid tests/golden/pending/005-mission-trigger/frame.grid 2>/dev/null || true; } | head -1 ); W="${LOCALAPPDATA:-/tmp}/Temp/oo_rkm_live.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); "$PY" tests/golden/mission_trigger.py --app-dir "$APP_DIR" --out "$NW/fresh.json" --frame-out "$NW/fresh.grid" --run-root "$NW/runs" >"$W/run.log" 2>&1; rc=$?; test $rc -eq 0 || { echo "FAIL: the live mission-trigger run exited rc=$rc:"; cat "$W/run.log"; rm -rf "$W"; exit 1; }; echo "  live run wall=$(( $(date +%s) - t0 ))s bytes=$(wc -c < "$W/fresh.json")"; "$PY" tests/golden/check_mission_trigger_evidence.py "$NW/fresh.json" --label "fresh run" || { echo "FAIL: the fresh run produced a dump with no proof a mission script fired from a world-script event"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$G" "$NW/fresh.json" --label-left "stored golden" --label-right "fresh run" || { echo "FAIL: a fresh run disagrees with the STORED golden. This is a FINDING, not a file to rewrite: investigate before re-blessing."; rm -rf "$W"; exit 1; }; "$PY" tests/golden/mission_trigger.py --compare-frame "$NW/fresh.grid" "$F" || { echo "FAIL: the freshly rendered frame is beyond the measured tolerance from the stored reference"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: a live run loads CloakingDevice.oolite-save, the systemWillPopulate world-script event fires oolite-cloaking-device which increments cloakcounter 6 ON DISK -> 7 IN THE ENGINE, registers its populator and spawns the ambush; the canonical dump is byte-identical to the stored golden and the rendered frame matches within the measured tolerance"
