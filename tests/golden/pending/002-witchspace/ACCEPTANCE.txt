export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); for f in tests/golden/witchspace_jump.py tests/golden/gate_002_spec.py tests/golden/gate_002_evidence.py; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; D=$( { ls -d goldens/windows-x64/002-witchspace tests/golden/pending/002-witchspace 2>/dev/null || true; } | head -1 ); test -n "$D" && test -d "$D" || { echo "MISSING: scenario 002 artifact dir (goldens/windows-x64/002-witchspace FIRST, tests/golden/pending/002-witchspace second - landing is a pure git mv of the directory as a unit)"; exit 1; }; for a in spec.json state.json provenance.json frame.grid; do test -f "$D/$a" || { echo "MISSING: $D/$a"; exit 1; }; done; test "$(wc -c < "$D/frame.grid")" -eq 4096 || { echo "FAIL: $D/frame.grid is $(wc -c < "$D/frame.grid") bytes, not the 4096-byte 64x64 luminance grid"; exit 1; }; "$PY" -m py_compile tests/golden/witchspace_jump.py tests/golden/gate_002_spec.py tests/golden/gate_002_evidence.py && echo "PASS: scenario 002-witchspace harness, both gates and all four artifacts present and importable (dir=$D)"
export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); OUT=$("$PY" tests/golden/gate_002_spec.py 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: gate_002_spec.py rc=$RC - the determinism knobs are not pinned, are not READ by witchspace_jump.py, or have drifted from the knobs this golden was blessed with. A fresh-run-vs-golden comparison CANNOT catch a changed seed because it moves both sides; re-bless deliberately rather than editing one side to match."; exit 1; }
export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); OUT=$("$PY" tests/golden/gate_002_evidence.py 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: gate_002_evidence.py rc=$RC - the BLESSED dump does not prove a witchspace jump HAPPENED (system changed, countdown observed, engine enter/exit events, fuel billed, arrival docked). A golden that only proves the game did not crash is the vacuity this scenario exists to exclude."; exit 1; }
export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); D=$( { ls -d goldens/windows-x64/002-witchspace tests/golden/pending/002-witchspace 2>/dev/null || true; } | head -1 ); test -n "$D" || { echo "MISSING: scenario 002 artifact dir"; exit 1; }; "$PY" -c 'import hashlib,json,sys,os;d=sys.argv[1];p=json.load(open(os.path.join(d,"provenance.json"),encoding="utf-8"));arts=p.get("artifacts") or {};rec=arts.get("state.json");sys.exit("FAIL: provenance records no artifacts.state.json digest; without a witness OUTSIDE state.json, editing the golden moves BOTH sides of every self-comparison and nothing goes red (bead oo-gxp)") if not rec else None;blob=open(os.path.join(d,"state.json"),"rb").read();sys.exit("FAIL: state.json is %d bytes but provenance witnesses %d; the golden and its witness were edited apart. A deliberate re-bless moves BOTH together and is governed by tools/rebless-approvals.txt."%(len(blob),rec["bytes"])) if len(blob)!=rec["bytes"] else None;got=hashlib.sha256(blob).hexdigest();sys.exit("FAIL: state.json hashes to %s but provenance witnesses %s. ONE QUANTISED UNIT on one float does this, and a golden-vs-copy comparison cannot see it. Investigate before re-blessing; do NOT update the digest to match."%(got,rec["sha256"])) if got!=rec["sha256"] else None;sys.exit("FAIL: provenance frame_digest_asserted=%r must be false - llvmpipe is NOT bit-reproducible (this scenario measured 10 distinct frame digests over ONE identical dump digest), so byte-gating the frame would flake forever. Deterministic artifacts get a digest; renderer output gets a measured floor. The asymmetry is the point."%p.get("frame_digest_asserted")) if p.get("frame_digest_asserted") is not False else None;fl=p.get("frame_liveness") or {};floor=fl.get("floor");sys.exit("FAIL: provenance frame_liveness=%r must assert a positive floor; liveness is the one frame property a dead run cannot fake"%(fl,)) if fl.get("asserted") is not True or not isinstance(floor,(int,float)) or floor<=0 else None;cells=open(os.path.join(d,"frame.grid"),"rb").read();spread=(max(cells)-min(cells))/255.0;sys.exit("FAIL: frame.grid luminance spread %.6f is below the blessed floor %.6f: the frame is (near-)uniform, which is what a run that never drew the scene produces"%(spread,floor)) if spread<floor else None;txt=blob.decode("utf-8");sys.exit("FAIL: state.json carries a frame digest; a frame hash inside the byte-compared dump makes the golden flake on renderer noise") if ("frame_hash" in txt or "frame_grid" in txt) else None;st=p.get("stability") or {};sys.exit("FAIL: provenance stability records %r run(s), %r dumped, %r refused, %r errored and %d distinct dump digest(s); the bead requires 10 runs producing ONE digest"%(st.get("runs"),st.get("dumps_written"),st.get("refused"),st.get("errored"),len(st.get("distinct_dump_digests") or []))) if int(st.get("runs",0))<10 or int(st.get("dumps_written",0))<10 or len(st.get("distinct_dump_digests") or [])!=1 else None;print("PASS: state.json is byte-for-byte the artifact provenance witnesses (%d bytes, sha256=%s) and %d/%d stability runs produced ONE dump digest with 0 refused / 0 errored. frame.grid spread %.4f >= floor %.4f (LIVENESS asserted) while its byte digest is deliberately NOT asserted over %d distinct frame digests from those same runs."%(len(blob),got,st["dumps_written"],st["runs"],spread,floor,st.get("distinct_frame_digests",0)))' "$D"
export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); D=$( { ls -d goldens/windows-x64/002-witchspace tests/golden/pending/002-witchspace 2>/dev/null || true; } | head -1 ); OUT=$("$PY" tests/golden/witchspace_jump.py --check-evidence "$D/state.json" --label "stored golden 002-witchspace" 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: the harness re-applying its OWN jump assertions to the stored golden rc=$RC"; exit 1; }; OUT=$("$PY" tests/golden/witchspace_jump.py --check-frame "$D/frame.grid" "$D/frame.grid" 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: the blessed frame fails the harness's own liveness check (rc=$RC). A golden that cannot witness itself is broken: check --check-frame's metric and floor agree with gate_002_evidence.py and provenance.json, and do NOT lower the floor to make it pass."; exit 1; }; T=$(mktemp -d); "$PY" -c 'import sys;open(sys.argv[1],"wb").write(bytes(4096))' "$T/zero.grid"; OUT=$("$PY" tests/golden/witchspace_jump.py --check-frame "$T/zero.grid" "$D/frame.grid" 2>&1); RC=$?; rm -rf "$T"; test $RC -ne 0 || { printf '%s\n' "$OUT"; echo "FAIL: an ALL-ZERO (never-drawn) frame PASSED the liveness check, so the check proves nothing about whether anything was rendered"; exit 1; }; echo "PASS: the stored dump re-passes the harness's own jump assertions, the blessed frame clears its liveness floor, and an undrawn all-zero frame is REFUSED (rc=$RC) - the liveness check has teeth"
export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test) or set OO_APP_DIR"; exit 1; }; D=$( { ls -d goldens/windows-x64/002-witchspace tests/golden/pending/002-witchspace 2>/dev/null || true; } | head -1 ); W="${LOCALAPPDATA:-/tmp}/Temp/oo_z22_accept.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); "$PY" tests/golden/witchspace_jump.py --app-dir "$APP_DIR" --out "$NW/fresh.json" --frame-out "$NW/fresh.grid" --run-root "$NW/runs" >"$W/run.log" 2>&1; rc=$?; cat "$W/run.log"; test $rc -eq 0 || { echo "FAIL: the live witchspace run exited rc=$rc after $(( $(date +%s)-t0 ))s. If this timed out on the console, check for ORPHANED oolite.exe processes first (ps -W | grep -i oolite) - one left by another run holds the port and makes a good run fail."; rm -rf "$W"; exit 1; }; echo "  live run wall=$(( $(date +%s)-t0 ))s bytes=$(wc -c < "$W/fresh.json")"; "$PY" tests/golden/witchspace_jump.py --check-evidence "$NW/fresh.json" --label "fresh run" || { echo "FAIL: the fresh run produced a dump that does not prove a jump happened"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$D/state.json" "$NW/fresh.json" --label-left "stored golden" --label-right "fresh run" || { echo "FAIL: a fresh run disagrees with the STORED golden. This is a FINDING, not a file to rewrite: investigate before re-blessing."; rm -rf "$W"; exit 1; }; "$PY" tests/golden/witchspace_jump.py --check-frame "$NW/fresh.grid" "$D/frame.grid" || { echo "FAIL: the freshly rendered frame did not clear the blessed liveness floor"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: a live run loads oolite-standard.oolite-save in Lave (7), begins the witchspace countdown, is carried through witchspace by the ENGINE to Zaonce (129) paying 56 tenths of fuel for a 5.6 LY jump, docks at the Icosahedron Station and runs 24 ticks there; the canonical dump is byte-identical to the stored golden and the rendered frame is live"
export PATH="/ucrt64/bin:$PATH"; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR"; exit 1; }; W="${LOCALAPPDATA:-/tmp}/Temp/oo_z22_mutant.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); OUT=$("$PY" tests/golden/witchspace_jump.py --app-dir "$APP_DIR" --out "$NW/broken.json" --run-root "$NW/runs" --break-jump 2>&1); rc=$?; printf '%s\n' "$OUT"; echo "  mutant arm wall=$(( $(date +%s)-t0 ))s rc=$rc"; test $rc -ne 0 || { echo "FAIL: --break-jump SKIPS THE COUNTDOWN so no jump can happen, yet the run PASSED. The scenario is then asserting nothing about the jump and would stay green if witchspace stopped working entirely."; rm -rf "$W"; exit 1; }; test ! -f "$NW/broken.json" || { echo "FAIL: the mutant run wrote a dump at $NW/broken.json; a run that fails its evidence assertions must not produce a blessable artifact"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: NON-VACUITY - with the countdown skipped the run FAILS (rc=$rc) because the ship never left Lave, and writes NO dump. The green above is caused by the jump, not by the absence of a crash."
