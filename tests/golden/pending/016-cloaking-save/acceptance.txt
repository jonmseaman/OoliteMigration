PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); for f in tests/golden/cloaking_load.py tests/golden/check_cloaking_evidence.py tests/golden/gate_016_spec.py tests/golden/test_cloaking_load.py; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; S=$( { ls tests/golden/scenarios/016-cloaking-save/spec.json tests/golden/pending/016-cloaking-save/spec.json 2>/dev/null || true; } | head -1 ); G=$( { ls goldens/windows-x64/016-cloaking-save/state.json tests/golden/pending/016-cloaking-save/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/016-cloaking-save/provenance.json tests/golden/pending/016-cloaking-save/provenance.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/016-cloaking-save/frame.grid tests/golden/pending/016-cloaking-save/frame.grid 2>/dev/null || true; } | head -1 ); for f in "$S" "$G" "$P" "$F"; do test -n "$f" && test -f "$f" || { echo "MISSING: scenario 016 spec/golden/provenance/frame.grid (spec resolves tests/golden/scenarios/ FIRST then tests/golden/pending/, exactly as cloaking_load.py SPEC_CANDIDATES does; state/provenance/frame resolve goldens/windows-x64/ FIRST then tests/golden/pending/, so landing is a pure git mv)"; exit 1; }; done; test -f upstream/oolite-tests/Checklist-files/Missions/CloakingDevice.oolite-save || { echo "MISSING: the CloakingDevice checklist save this scenario loads"; exit 1; }; test -f upstream/oolite-tests/Checklist-files/Missions/Trumbles.oolite-save || { echo "MISSING: the sibling checklist save used as the wrong-save control"; exit 1; }; test "$(wc -c < "$F")" -eq 4096 || { echo "FAIL: $F is $(wc -c < "$F") bytes, not the 4096-byte 64x64 luminance grid"; exit 1; }; "$PY" -m py_compile tests/golden/cloaking_load.py tests/golden/check_cloaking_evidence.py tests/golden/gate_016_spec.py tests/golden/test_cloaking_load.py && echo "PASS: scenario 016-cloaking-save harness, evidence checker, spec gate, tests, spec, golden, provenance, frame grid, the CloakingDevice fixture and the Trumbles control are all present and importable (spec=$S golden=$G prov=$P frame=$F)"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" tests/golden/gate_016_spec.py
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c 'import plistlib,json,os,sys;S=[q for q in ("tests/golden/scenarios/016-cloaking-save/spec.json","tests/golden/pending/016-cloaking-save/spec.json") if os.path.isfile(q)][0];s=json.load(open(S,encoding="utf-8"));p=os.path.join(*s["load_save"].split("/"));d=plistlib.load(open(p,"rb"));sys.exit("FAIL: %s written_by_version=%r, but scenario 016 IS the %s save-format compatibility contract. If upstream replaced the fixture that is a FINDING: re-measure, do NOT edit the assertion to match."%(p,d.get("written_by_version"),s["save_format_version"])) if str(d.get("written_by_version"))!=s["save_format_version"] else None;mv=d.get("mission_variables");sys.exit("FAIL: %s has no mission_variables dict"%p) if not isinstance(mv,dict) else None;want=sorted(s["expected_mission_variable_keys"]);sys.exit("FAIL: %s carries mission variable keys %r, not the %d the spec pins %r. PRESENCE is witnessed as a KEY SET, never as a value: A MISSION VARIABLE THAT IS THE EMPTY STRING READS IDENTICALLY TO AN ABSENT ONE, so a value comparison cannot tell a dropped key from a blanked one."%(p,sorted(mv),len(want),want)) if sorted(mv)!=want else None;k="mission_cloakcounter";sys.exit("FAIL: %s does not carry %r, which the bead names as the field the canonical dump must include"%(p,k)) if k not in mv else None;dz=s["mission_variable_write_exceptions"][k];sys.exit("FAIL: %s %s is %r ON DISK but the spec pins %r"%(p,k,int(mv[k]),dz["in_file"])) if int(mv[k])!=dz["in_file"] else None;eq=d.get("extra_equipment") or {};sys.exit("FAIL: %s restores %d equipment key(s), spec pins %d"%(p,len(eq),s["expected_equipment_count"])) if len(eq)!=s["expected_equipment_count"] else None;sys.exit("FAIL: %s no longer carries EQ_ENERGY_BOMB. That legacy item is what makes the engine-side migration evaluable at all; without it the whole declared-migration clause is unevaluable and would pass vacuously."%p) if "EQ_ENERGY_BOMB" not in eq else None;miss=[f["plist"] for f in s["census"] if f["plist"] not in d];sys.exit("FAIL: %s is missing %d census key(s) %r; the census describes a save this file is not"%(p,len(miss),miss)) if miss else None;c=os.path.join(*s["control_save"].split("/"));cd=plistlib.load(open(c,"rb"));cmv=cd.get("mission_variables") or {};sys.exit("FAIL: the control %s carries the SAME mission variable key set as the subject, so the key-set presence defence could not tell the two apart"%c) if sorted(cmv)==sorted(mv) else None;sys.exit("FAIL: the control %s is a %r-era save; a control that differs in ERA as well as CONTENTS cannot isolate the contents"%(c,cd.get("written_by_version"))) if str(cd.get("written_by_version"))!=s["save_format_version"] else None;sys.exit("FAIL: the control %s is in the same system as the subject; the cheap system pin could not distinguish them"%c) if str(cd.get("current_system_name"))==str(d.get("current_system_name")) else None;print("PASS: the fixture is a %s-era save for commander %r in galaxy %d / system %r carrying %d mission variable key(s) including %s=%d ON DISK, %d equipment key(s) including the legacy EQ_ENERGY_BOMB, and every one of the %d census key(s); the control %s is the SAME era but a different commander in %r with a different key set, so the two arms differ in CONTENTS only"%(d["written_by_version"],d["player_name"],int(d["galaxy_number"]),d["current_system_name"],len(mv),k,int(mv[k]),len(eq),len(s["census"]),os.path.basename(c),cd["current_system_name"]))'
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -m pytest tests/golden/test_cloaking_load.py -q -p no:cacheprovider
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$( { ls goldens/windows-x64/016-cloaking-save/state.json tests/golden/pending/016-cloaking-save/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/016-cloaking-save/provenance.json tests/golden/pending/016-cloaking-save/provenance.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/016-cloaking-save/frame.grid tests/golden/pending/016-cloaking-save/frame.grid 2>/dev/null || true; } | head -1 ); OUT=$("$PY" tests/golden/check_cloaking_evidence.py "$G" --label "stored golden 016-cloaking-save" 2>&1); RC=$?; printf "%s\n" "$OUT"; test $RC -eq 0 || { echo "FAIL: the stored golden does not prove a 1.75 save round-tripped; a byte-identical comparison against it would be vacuous"; exit 1; }; "$PY" -c 'import hashlib,json,sys;g,p,f=sys.argv[1],sys.argv[2],sys.argv[3];blob=open(g,"rb").read();prov=json.load(open(p,encoding="utf-8"));arts=prov.get("artifacts") or {};rec=arts.get("state.json");sys.exit("FAIL: %s records no artifacts.state.json digest. A GOLDEN CANNOT WITNESS ITSELF: without a digest in a SEPARATE file, editing the golden moves BOTH sides of every self-comparison and no line can see it (bead oo-gxp)."%p) if not rec else None;sys.exit("FAIL: the stored golden is %d bytes but provenance records %d; one of the two was edited alone. A deliberate re-bless moves BOTH files together."%(len(blob),rec["bytes"])) if len(blob)!=rec["bytes"] else None;got=hashlib.sha256(blob).hexdigest();sys.exit("FAIL: the stored golden hashes to sha256=%s but provenance witnesses sha256=%s. ONE QUANTISED UNIT on one float is enough to do this. Investigate before re-blessing; do NOT update the digest to match."%(got,rec["sha256"])) if got!=rec["sha256"] else None;fb=open(f,"rb").read();frec=arts.get("frame.grid") or {};sys.exit("FAIL: frame.grid is %d bytes but provenance records %r"%(len(fb),frec.get("bytes"))) if len(fb)!=frec.get("bytes") else None;sys.exit("FAIL: provenance records no note explaining why frame.grid is NOT byte-gated; without the written reason the next agent byte-hashes the frame and the gate starts flaking on renderer noise") if not str(frec.get("note") or "").strip() else None;txt=blob.decode("utf-8");sys.exit("FAIL: the dump carries a frame digest. llvmpipe is NOT bit-reproducible: this scenario 10-run sweep produced TEN DISTINCT frame-grid digests over TEN BYTE-IDENTICAL dumps, so a frame hash inside state.json would make the golden comparison flake on renderer noise.") if ("frame_hash" in txt or "frame_grid" in txt) else None;d=json.loads(txt);sys.exit("FAIL: the golden carries no top-level mission_variables; the bead requires the canonical dump itself to carry the mission state") if not isinstance(d.get("mission_variables"),dict) else None;sys.exit("FAIL: the golden top-level mission_variables does not carry mission_cloakcounter, which the bead names explicitly") if "mission_cloakcounter" not in d["mission_variables"] else None;ev=d.get("evidence") or {};sys.exit("FAIL: the dump top-level mission_variables %r disagrees with evidence %r; one was edited alone"%(d["mission_variables"],ev.get("mission_variables"))) if d["mission_variables"]!=ev.get("mission_variables") else None;sys.path.insert(0,"tests/golden");import frame_hash;tol=frame_hash.derive_tolerance();sys.exit("FAIL: frame_hash.TOLERANCE has moved from the value bead oo-ae9 measured (now %r); re-measure with calibrate.py, do NOT adjust it to make a run pass"%tol) if abs(tol-0.004377268476873758)>1e-12 else None;ft=prov.get("frame_tolerance") or {};sys.exit("FAIL: provenance frame_tolerance.tolerance=%r is not the measured %r"%(ft.get("tolerance"),tol)) if abs(float(ft.get("tolerance",0))-tol)>1e-12 else None;sys.exit("FAIL: provenance same_scene_max=%r is at or beyond the measured tolerance %r; the frame comparison would flake"%(ft.get("same_scene_max"),tol)) if not float(ft["same_scene_max"])<tol else None;sys.exit("FAIL: the wrong-save control frame sits %.2fx the measured tolerance; at or below 2x the frame does not discriminate this scenario from a DIFFERENT checklist save and asserting on it would be decoration"%(float(ft["control_wrong_save_distance"])/tol)) if float(ft["control_wrong_save_distance"])/tol<=2 else None;lv=prov.get("frame_liveness") or {};sys.exit("FAIL: provenance frame_liveness.asserted=%r; liveness is the frame property a DEAD run fails - a run that died before drawing yields an all-black grid at distance 0 and is otherwise indistinguishable by exit code"%lv.get("asserted")) if lv.get("asserted") is not True else None;dist=frame_hash.distance(fb,bytes(frame_hash.GRID_CELLS));sys.exit("FAIL: the stored frame.grid is %.6f from all-black, under the recorded liveness floor %r; it does not show anything was RENDERED"%(dist,lv.get("floor"))) if dist<float(lv["floor"]) else None;st=prov.get("stability") or {};r=int(st.get("runs",0));acc=int(st.get("dumps_written",0))+int(st.get("refused",0))+int(st.get("errored",0));sys.exit("FAIL: provenance stability accounts for %d outcome(s) across %d run(s); EVERY run must be accounted for, and a REFUSAL is not a DIFFERENCE"%(acc,r)) if acc!=r else None;sys.exit("FAIL: provenance records %r run(s) in %d distinct dump digest(s); the bead requires 10 runs producing exactly ONE"%(st.get("runs"),len(st.get("distinct_dump_digests") or []))) if r<10 or len(st.get("distinct_dump_digests") or [])!=1 else None;sys.exit("FAIL: the blessed golden digest %s is not the one the stability sweep agreed on %r"%(got,st["distinct_dump_digests"])) if got!=st["distinct_dump_digests"][0] else None;sys.exit("FAIL: provenance records %d errored run(s); an errored run has no verdict and cannot support a stability claim"%st["errored"]) if st["errored"] else None;dm=prov.get("declared_migrations") or {};S=[q for q in ("tests/golden/scenarios/016-cloaking-save/spec.json","tests/golden/pending/016-cloaking-save/spec.json") if __import__("os").path.isfile(q)][0];spec=json.load(open(S,encoding="utf-8"));[sys.exit("FAIL: provenance declared_migrations.%s=%r disagrees with spec %r. The declared set is the ONE place a failing run could be made to pass by widening it, so it is witnessed from OUTSIDE the spec."%(n,sorted(dm.get(n) or []),sorted(spec[n]))) for n in ("census_migrations","mission_variable_write_exceptions","equipment_migrations") if sorted(dm.get(n) or [])!=sorted(spec[n])];print("PASS: the stored golden is byte-for-byte the artifact provenance witnesses (%d bytes, sha256=%s) and is the digest all %d stability runs agreed on (%d dumped / %d refused / %d errored, every run accounted for). It carries mission_variables incl. mission_cloakcounter at top level agreeing with evidence. NOTE THE DELIBERATE ASYMMETRY: frame.grid is recorded but NOT byte-gated - the sweep produced %d DISTINCT frame digests over ONE dump digest - so the frame is held to the measured tolerance %.6f, inside which two launches sit (max %.6f, %.2fx) while a DIFFERENT checklist save is far outside it (%.6f, %.2fx), and to a LIVENESS floor the stored frame clears at %.4f vs %.4f."%(len(blob),got,st["runs"],st["dumps_written"],st["refused"],st["errored"],st["distinct_frame_grid_digests"],tol,ft["same_scene_max"],float(ft["same_scene_max"])/tol,ft["control_wrong_save_distance"],float(ft["control_wrong_save_distance"])/tol,dist,float(lv["floor"])))' "$G" "$P" "$F"
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; G=$( { ls goldens/windows-x64/016-cloaking-save/state.json tests/golden/pending/016-cloaking-save/state.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/016-cloaking-save/frame.grid tests/golden/pending/016-cloaking-save/frame.grid 2>/dev/null || true; } | head -1 ); W="${LOCALAPPDATA:-/tmp}/Temp/oo_ghhw_live.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); "$PY" tests/golden/cloaking_load.py --app-dir "$APP_DIR" --out "$NW/fresh.json" --frame-out "$NW/fresh.grid" --run-root "$NW/runs" >"$W/run.log" 2>&1; rc=$?; test $rc -eq 0 || { echo "FAIL: the live cloaking-save run exited rc=$rc:"; cat "$W/run.log"; rm -rf "$W"; exit 1; }; echo "  live run wall=$(( $(date +%s) - t0 ))s bytes=$(wc -c < "$W/fresh.json")"; test $(( $(date +%s) - t0 )) -ge 5 || { echo "FAIL: the run returned in under 5s. A LIVE RED-PROOF MUST ACTUALLY LAUNCH THE GAME: a measured launch of this scenario takes 12-24s, so this did not launch it."; rm -rf "$W"; exit 1; }; "$PY" tests/golden/check_cloaking_evidence.py "$NW/fresh.json" --label "fresh run" || { echo "FAIL: the fresh run produced a dump with no proof a 1.75 save round-tripped"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$G" "$NW/fresh.json" --label-left "stored golden" --label-right "fresh run" || { echo "FAIL: a fresh run disagrees with the STORED golden. This is a FINDING, not a file to rewrite: investigate before re-blessing."; rm -rf "$W"; exit 1; }; "$PY" tests/golden/cloaking_load.py --compare-frame "$NW/fresh.grid" "$F" || { echo "FAIL: the freshly rendered frame is beyond the measured tolerance from the stored reference"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: a live run loads the 1.75-era CloakingDevice.oolite-save through the game -load argument; 13/15 census fields and 9/10 mission variables are identical across two independent readers (plistlib in this process vs JS in the game process), all 16 saved equipment keys are accounted for, and the ONLY differences are the three DECLARED migrations with their pinned deltas and the loader own log testimony. The canonical dump is byte-identical to the stored golden and the rendered frame matches within the measured tolerance."
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; W="${LOCALAPPDATA:-/tmp}/Temp/oo_ghhw_red.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); C=upstream/oolite-tests/Checklist-files/Missions/Trumbles.oolite-save; t0=$(date +%s); "$PY" tests/golden/cloaking_load.py --app-dir "$APP_DIR" --expect-from "$C" --out "$NW/red.json" --frame-out "$NW/red.grid" --run-root "$NW/runs" >"$W/red.log" 2>&1; rc=$?; W1=$(( $(date +%s) - t0 )); test $rc -ne 0 || { echo "FAIL: the game loaded the REAL CloakingDevice save while the expectations came from the SIBLING Trumbles save, and the scenario still PASSED. The round-trip comparison is not comparing anything."; cat "$W/red.log"; rm -rf "$W"; exit 1; }; test ! -f "$NW/red.json" || { echo "FAIL: the scenario refused but wrote a dump anyway; a refusing run must leave no artifact a later step could mistake for a verdict"; rm -rf "$W"; exit 1; }; test $W1 -ge 5 || { echo "FAIL: the red-proof arm returned in ${W1}s. A LIVE RED-PROOF MUST ACTUALLY LAUNCH THE GAME - it did not."; cat "$W/red.log"; rm -rf "$W"; exit 1; }; echo "  wrong-expectations arm refused in ${W1}s: $(grep -m1 -E 'REFUS|FAIL|not the' "$W/red.log" | cut -c1-160)"; rm -rf "$W"; echo "PASS: the sharper red-proof arm is LIVE and RED. The game really launched (${W1}s, not a sub-second no-op) and loaded the CORRECT save, so the cheap system pin CANNOT be what fired; the refusal comes from the round-trip comparison itself being fed expectations from a SIBLING checklist save of the SAME 1.75 era, and no dump was written."
