PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); for f in tests/golden/trade_cycle.py tests/golden/check_trade_cycle_evidence.py tests/golden/gate_004_spec.py tests/golden/test_trade_cycle.py; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; S=$( { ls tests/golden/scenarios/004-trade-cycle/spec.json tests/golden/pending/004-trade-cycle/spec.json 2>/dev/null || true; } | head -1 ); G=$( { ls goldens/windows-x64/004-trade-cycle/state.json tests/golden/pending/004-trade-cycle/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/004-trade-cycle/provenance.json tests/golden/pending/004-trade-cycle/provenance.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/004-trade-cycle/frame.grid tests/golden/pending/004-trade-cycle/frame.grid 2>/dev/null || true; } | head -1 ); for f in "$S" "$G" "$P" "$F"; do test -n "$f" && test -f "$f" || { echo "MISSING: scenario 004 spec/golden/provenance/frame.grid (guarded goldens/ path searched first, then tests/golden/pending/)"; exit 1; }; done; test -f upstream/oolite-tests/Checklist-files/Missions/CloakingDevice.oolite-save || { echo "MISSING: the CloakingDevice checklist save this scenario loads"; exit 1; }; test "$(wc -c < "$F")" -eq 4096 || { echo "FAIL: $F is $(wc -c < "$F") bytes, not the 4096-byte 64x64 luminance grid"; exit 1; }; "$PY" -m py_compile tests/golden/trade_cycle.py tests/golden/check_trade_cycle_evidence.py tests/golden/gate_004_spec.py tests/golden/test_trade_cycle.py && echo "PASS: scenario 004-trade-cycle script, evidence checker, spec gate, tests, spec, golden, provenance, frame grid and the .oolite-save fixture are all present and importable (spec=$S golden=$G prov=$P frame=$F)"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); OUT=$("$PY" tests/golden/gate_004_spec.py 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: scenario 004's determinism knobs are not pinned, not read where they act, or disagree with the knobs the golden was blessed with. A fresh-run-vs-golden comparison CANNOT catch a changed knob, because changing it moves BOTH sides."; exit 1; }
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$( { ls goldens/windows-x64/004-trade-cycle/state.json tests/golden/pending/004-trade-cycle/state.json 2>/dev/null || true; } | head -1 ); OUT=$("$PY" tests/golden/check_trade_cycle_evidence.py "$G" --label "stored golden 004-trade-cycle" 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: the stored golden does not prove a trade cycle happened; a byte-identical comparison against it would be vacuous - it would pass just as well against a run in which no money, no cargo and no market quantity ever moved."; exit 1; }
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$( { ls goldens/windows-x64/004-trade-cycle/state.json tests/golden/pending/004-trade-cycle/state.json 2>/dev/null || true; } | head -1 ); P=$( { ls goldens/windows-x64/004-trade-cycle/provenance.json tests/golden/pending/004-trade-cycle/provenance.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/004-trade-cycle/frame.grid tests/golden/pending/004-trade-cycle/frame.grid 2>/dev/null || true; } | head -1 ); "$PY" -c 'import hashlib,json,sys;g,p,f=sys.argv[1],sys.argv[2],sys.argv[3];blob=open(g,"rb").read();prov=json.load(open(p,encoding="utf-8"));arts=prov.get("artifacts") or {};rec=arts.get("state.json");sys.exit("FAIL: %s records no artifacts.state.json digest; without an INDEPENDENT witness, editing the golden changes BOTH sides of every self-comparison and no line can see it (bead oo-gxp)"%p) if not rec else None;got=hashlib.sha256(blob).hexdigest();sys.exit("FAIL: the stored golden is %d bytes but provenance records %d. The golden and the provenance that witnesses it have diverged; one of them was edited alone. A deliberate re-bless moves BOTH files together and is governed by tools/rebless-approvals.txt."%(len(blob),rec["bytes"])) if len(blob)!=rec["bytes"] else None;sys.exit("FAIL: the stored golden hashes to sha256=%s but provenance witnesses sha256=%s. ONE QUANTISED UNIT on one price is enough to do this, and a fresh-run-vs-golden or golden-vs-copy comparison CANNOT catch it because the edit moves both sides. Investigate before re-blessing; do NOT update the digest to match."%(got,rec["sha256"])) if got!=rec["sha256"] else None;frec=arts.get("frame.grid");sys.exit("FAIL: provenance records no artifacts[\"frame.grid\"] entry") if not frec else None;fb=open(f,"rb").read();sys.exit("FAIL: frame.grid is %d bytes but provenance records %d"%(len(fb),frec["bytes"])) if len(fb)!=frec["bytes"] else None;sys.exit("FAIL: provenance frame.grid note does not say the digest is never a gate predicate; llvmpipe is NOT bit-reproducible (this sweep produced %r distinct grid digests over byte-identical dumps) so byte-gating the frame would make the gate flake on renderer noise"%prov.get("stability",{}).get("distinct_frame_grid_digests")) if "NEVER a gate predicate" not in frec.get("note","") else None;txt=blob.decode("utf-8");sys.exit("FAIL: the dump carries a frame digest. llvmpipe is NOT bit-reproducible across runs, so a frame hash inside state.json makes the byte-identical golden comparison flake on renderer noise; frames are compared with the MEASURED tolerance and stored beside the dump as frame.grid.") if ("frame_hash" in txt or "frame_grid" in txt) else None;st=prov.get("stability") or {};sys.exit("FAIL: provenance records %r stability run(s), %r dumped + %r refused, %r errored, %d distinct digest(s); a golden may be blessed only from a sweep of at least 10 that accounted for every run and agreed with itself"%(st.get("runs"),st.get("dumps_written"),st.get("refused"),st.get("errored"),len(st.get("distinct_dump_digests") or []))) if not (st.get("runs",0)>=10 and st.get("errored")==0 and st.get("dumps_written",0)+st.get("refused",0)==st.get("runs") and len(st.get("distinct_dump_digests") or [])==1 and len(st.get("per_run") or [])==st.get("runs")) else None;sys.path.insert(0,"tests/golden");import frame_hash;tol=frame_hash.derive_tolerance();sys.exit("FAIL: frame_hash.TOLERANCE has moved from the value bead oo-ae9 measured (now %r); re-measure with calibrate.py and re-bless, do NOT adjust it to make a run pass"%tol) if abs(tol-0.004377268476873758)>1e-12 else None;ft=prov.get("frame_tolerance") or {};sys.exit("FAIL: provenance frame_tolerance.tolerance=%r is not the measured tolerance %r"%(ft.get("tolerance"),tol)) if abs(float(ft.get("tolerance",0))-tol)>1e-12 else None;fm=prov.get("frame_measurements") or {};ss=fm.get("same_scene_pairs_10_runs") or {};nd=fm.get("different_scene_015_trumbles") or {};sys.exit("FAIL: provenance records a worst same-scene frame distance of %r, at or beyond the measured tolerance %r - this scenario may not assert frame equality on that data (scenario 015 measured exactly this and correctly refused)"%(ss.get("max"),tol)) if not float(ss.get("max",1))<tol else None;sys.exit("FAIL: the nearest DIFFERENT scene is %r away and the worst SAME-scene pair is %r; with signal not well above noise the frame cannot discriminate and asserting it would be a gate that passes for the wrong reasons"%(nd.get("distance"),ss.get("max"))) if not float(nd.get("distance",0))>4*float(ss.get("max",1)) else None;ev=json.loads(txt).get("evidence") or {};sys.exit("FAIL: the golden records untraded_goods_changed=%r; the market has a carrier other than this scenario, so the observable is shared and the gate is a race (bead oo-rkm)"%ev.get("untraded_goods_changed")) if ev.get("untraded_goods_changed") else None;print("PASS: the stored golden is byte-for-byte the artifact provenance witnesses (%d bytes, sha256=%s), blessed from a %d-run sweep with a single dump digest and every run accounted for. NOTE THE DELIBERATE ASYMMETRY: frame.grid is recorded in provenance but NOT byte-gated - llvmpipe is not bit-reproducible (%r distinct grid digests over byte-identical dumps) - so the frame is judged by the measured tolerance %.6f, inside which this scenario sits (worst same-scene pair %.6f, %.2fx) while the nearest different scene is outside it (%.6f, %.2fx)."%(len(blob),got,st["runs"],st.get("distinct_frame_grid_digests"),tol,ss["max"],ss["max"]/tol,nd["distance"],nd["distance"]/tol))' "$G" "$P" "$F"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -m pytest tests/golden/test_trade_cycle.py -q -p no:cacheprovider
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; G=$( { ls goldens/windows-x64/004-trade-cycle/state.json tests/golden/pending/004-trade-cycle/state.json 2>/dev/null || true; } | head -1 ); F=$( { ls goldens/windows-x64/004-trade-cycle/frame.grid tests/golden/pending/004-trade-cycle/frame.grid 2>/dev/null || true; } | head -1 ); W="${LOCALAPPDATA:-/tmp}/Temp/oo_zv2_live.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); "$PY" tests/golden/trade_cycle.py --app-dir "$APP_DIR" --out "$NW/fresh.json" --frame-out "$NW/fresh.grid" --run-root "$NW/runs" > "$W/run.log" 2>&1; rc=$?; test $rc -eq 0 || { echo "FAIL: the live trade-cycle run exited rc=$rc:"; cat "$W/run.log"; rm -rf "$W"; exit 1; }; cat "$W/run.log"; echo "  live run wall=$(( $(date +%s) - t0 ))s bytes=$(wc -c < "$W/fresh.json")"; "$PY" tests/golden/check_trade_cycle_evidence.py "$NW/fresh.json" --label "fresh run" || { echo "FAIL: the fresh run produced a dump with no proof a trade cycle happened"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$G" "$NW/fresh.json" --label-left "stored golden" --label-right "fresh run" || { echo "FAIL: a fresh run disagrees with the STORED golden. This is a FINDING, not a file to rewrite: investigate before re-blessing."; rm -rf "$W"; exit 1; }; "$PY" tests/golden/trade_cycle.py --compare-frame "$NW/fresh.grid" "$F" || { echo "FAIL: the freshly rendered frame is beyond the measured tolerance from the stored reference, or is not live at all"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: a live run loads CloakingDevice.oolite-save, buys 5 food, holds it 3.0s of game time, sells 3 back, and proves every leg against the engine's own answers (market quantity, hold, engine-recomputed cargoSpaceUsed, and a purse moving by exactly price*units/10); its canonical dump is byte-identical to the stored golden and its rendered frame matches the stored reference within the measured tolerance"
