PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); for f in tests/golden/constrictor_save.py tests/golden/check_constrictor_evidence.py tests/golden/test_constrictor_save.py tests/golden/scenario_013.sh; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; S=$(ls tests/golden/scenarios/013-constrictor/spec.json tests/golden/pending/013-constrictor/spec.json 2>/dev/null | head -1); G=$(ls goldens/windows-x64/013-constrictor/state.json tests/golden/pending/013-constrictor/state.json 2>/dev/null | head -1); P=$(ls goldens/windows-x64/013-constrictor/provenance.json tests/golden/pending/013-constrictor/provenance.json 2>/dev/null | head -1); F=$(ls goldens/windows-x64/013-constrictor/frame.grid tests/golden/pending/013-constrictor/frame.grid 2>/dev/null | head -1); for f in "$S" "$G" "$P" "$F"; do test -n "$f" && test -f "$f" || { echo "MISSING: scenario 013 spec/golden/provenance/frame.grid (guarded goldens/ path searched first, then tests/golden/pending/)"; exit 1; }; done; test -f upstream/oolite-tests/Checklist-files/Missions/Constrictor.oolite-save || { echo "MISSING: the Constrictor checklist save this scenario loads"; exit 1; }; test "$(wc -c < "$F")" -eq 4096 || { echo "FAIL: $F is $(wc -c < "$F") bytes, not the 4096-byte 64x64 luminance grid"; exit 1; }; "$PY" -m py_compile tests/golden/constrictor_save.py tests/golden/check_constrictor_evidence.py tests/golden/test_constrictor_save.py && echo "PASS: scenario 013-constrictor script, evidence checker, tests, spec, golden, provenance, frame grid and the .oolite-save fixture are all present and importable (spec=$S golden=$G prov=$P frame=$F)"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c 'import json,os,sys;S=[q for q in ("tests/golden/scenarios/013-constrictor/spec.json","tests/golden/pending/013-constrictor/spec.json") if os.path.isfile(q)][0];P=[q for q in ("goldens/windows-x64/013-constrictor/provenance.json","tests/golden/pending/013-constrictor/provenance.json") if os.path.isfile(q)][0];s=json.load(open(S,encoding="utf-8"));p=json.load(open(P,encoding="utf-8"));req=["seed","system_id","ticks","tick_seconds","quant_decimals","load_save","control_save","mission_script_name","expected_commander_name","expected_score","expected_mission_variables","expected_mission_conhunt","expected_mission_conhunt_present","expected_live_mission_handlers"];miss=[k for k in req if k not in s];sys.exit("FAIL: spec.json does not pin %s; without them the scenario is not reproducible"%miss) if miss else None;sys.exit("FAIL: spec quant_decimals=%r but the storage policy is 3"%s["quant_decimals"]) if s["quant_decimals"]!=3 else None;sys.exit("FAIL: spec ticks=%r, must be a positive int"%s["ticks"]) if not isinstance(s["ticks"],int) or s["ticks"]<1 else None;sys.exit("FAIL: spec tick_seconds=%r, must be a positive number"%s["tick_seconds"]) if not isinstance(s["tick_seconds"],(int,float)) or s["tick_seconds"]<=0 else None;sys.exit("FAIL: spec load_save=%r, expected the Constrictor checklist save"%s["load_save"]) if not s["load_save"].endswith("Missions/Constrictor.oolite-save") else None;sys.exit("FAIL: spec control_save=%r is the same file as load_save; a differential whose two arms are the same file cannot move"%s["control_save"]) if s["control_save"]==s["load_save"] else None;sys.exit("FAIL: spec expected_score=%r but the fixture pins 255, and that EXACT value is why mission_conhunt is absent (the mission script fires above 255)"%s["expected_score"]) if s["expected_score"]!=255 else None;sys.exit("FAIL: spec expected_mission_conhunt_present=%r; this fixture is PRE-mission and conhunt is measured ABSENT"%s["expected_mission_conhunt_present"]) if s["expected_mission_conhunt_present"] is not False else None;sys.exit("FAIL: spec expected_mission_variables=%r, not the measured set"%s["expected_mission_variables"]) if s["expected_mission_variables"]!={"CT_thargonCount":0,"snoopers_CRCNews":"|","snoopers_usedSlots":0,"trumbles":"NOT_NOW"} else None;sys.exit("FAIL: spec expected_live_mission_handlers=%r, not the measured three"%s["expected_live_mission_handlers"]) if sorted(s["expected_live_mission_handlers"])!=["guiScreenChanged","missionScreenOpportunity","systemWillPopulate"] else None;src=open("tests/golden/constrictor_save.py",encoding="utf-8").read();[sys.exit("FAIL: constrictor_save.py never reads spec[%r]; the knob is decoration - if someone changed it, no line would go red"%k) for k in req if k!="quant_decimals" and "spec["+chr(34)+k+chr(34)+"]" not in src];kn=p.get("scenario_knobs");sys.exit("FAIL: %s records no scenario_knobs; the seed/system/ticks the golden was BLESSED with are unrecorded, so nothing can detect the spec drifting away from them (bead oo-3ya)"%P) if not kn else None;[sys.exit("FAIL: provenance scenario_knobs does not record %r"%k) for k in ("seed","system_id","ticks","tick_seconds","quant_decimals") if k not in kn];drift={k:(kn[k],s.get(k)) for k in kn if s.get(k)!=kn[k]};sys.exit("FAIL: spec.json disagrees with the knobs this golden was blessed with (%s records blessed-vs-spec %r). A fresh-run-vs-golden comparison CANNOT catch this, because changing a knob changes BOTH sides. Re-bless deliberately or restore the spec - do NOT edit one side to match."%(P,drift)) if drift else None;sys.exit("FAIL: provenance quant_decimals=%r but the policy is 3"%p.get("quant_decimals")) if p.get("quant_decimals")!=3 else None;sys.exit("FAIL: provenance dump_tool=%r, expected tests/golden/constrictor_save.py"%p.get("dump_tool")) if p.get("dump_tool")!="tests/golden/constrictor_save.py" else None;print("PASS: seed=%s system_id=%s ticks=%s tick_seconds=%s quant=3 save=%s control=%s, all %d knob(s) read by constrictor_save.py and all %d equal to the values recorded in the golden provenance"%(s["seed"],s["system_id"],s["ticks"],s["tick_seconds"],s["load_save"].split("/")[-1],s["control_save"].split("/")[-1],len(req),len(kn)))'
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -c 'import plistlib,json,os,sys;S=[q for q in ("tests/golden/scenarios/013-constrictor/spec.json","tests/golden/pending/013-constrictor/spec.json") if os.path.isfile(q)][0];s=json.load(open(S,encoding="utf-8"));p=os.path.join(*s["load_save"].split("/"));d=plistlib.load(open(p,"rb"));mv=d.get("mission_variables");sys.exit("FAIL: %s has no mission_variables; the whole point of this scenario is mission state"%p) if not isinstance(mv,dict) else None;sys.exit("FAIL: %s now carries mission_conhunt=%r. This scenario asserts its ABSENCE, measured from ship_kills being exactly 255 and oolite-constrictor-hunt-mission.js firing only above 255. If upstream changed the fixture this is a FINDING: re-measure, do NOT edit the assertion to match."%(p,mv.get("mission_conhunt"))) if "mission_conhunt" in mv else None;sys.exit("FAIL: %s ship_kills is %r but the spec pins %r, and that exact value is WHY conhunt is absent"%(p,d.get("ship_kills"),s["expected_score"])) if int(d["ship_kills"])!=int(s["expected_score"]) else None;sys.exit("FAIL: %s player_name=%r, expected %r"%(p,d.get("player_name"),s["expected_commander_name"])) if str(d.get("player_name"))!=s["expected_commander_name"] else None;sys.exit("FAIL: %s written_by_version=%r, expected the 1.75-era fixture this scenario pins as the save-format compatibility contract"%(p,d.get("written_by_version"))) if str(d.get("written_by_version"))!="1.75" else None;c=os.path.join(*s["control_save"].split("/"));cd=plistlib.load(open(c,"rb"));cmv=cd.get("mission_variables") or {};sys.exit("FAIL: the control save %s does NOT carry mission_conhunt, so the differential arm cannot show the mission variable moving"%c) if "mission_conhunt" not in cmv else None;print("PASS: the fixture is a %s-era save for commander %r with ship_kills=%d and %d mission variable(s) NOT including mission_conhunt (pre-mission, as measured), while the control save %s carries mission_conhunt=%r - so the two arms differ in the field this scenario is about"%(d["written_by_version"],d["player_name"],int(d["ship_kills"]),len(mv),os.path.basename(c),cmv["mission_conhunt"]))'
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -m pytest tests/golden/test_constrictor_save.py -q -p no:cacheprovider
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$(ls goldens/windows-x64/013-constrictor/state.json tests/golden/pending/013-constrictor/state.json 2>/dev/null | head -1); OUT=$("$PY" tests/golden/check_constrictor_evidence.py "$G" --label "stored golden 013-constrictor" 2>&1); RC=$?; printf '%s\n' "$OUT"; test $RC -eq 0 || { echo "FAIL: the stored golden does not prove the Constrictor save's mission state was loaded; a byte-identical comparison against it would be vacuous - it would pass just as well against a default new commander with no mission"; exit 1; }; W="${LOCALAPPDATA:-/tmp}/Temp/oo_5h8e_gate.$$"; rm -rf "$W"; mkdir -p "$W"; cp "$G" "$W/copy.json"; C=$(cygpath -m "$W/copy.json" 2>/dev/null || echo "$W/copy.json"); "$PY" tests/golden/golden_diff.py "$G" "$C" --label-left "stored golden" --label-right "an untouched copy"; rc=$?; test $rc -eq 0 || { echo "FAIL: golden_diff refuses or rejects the stored golden (rc=$rc); it is off-policy or collapsed"; rm -rf "$W"; exit 1; }; out2=$("$PY" tests/golden/golden_diff.py "$G" "$G" 2>&1); rc2=$?; rm -rf "$W"; test $rc2 -eq 2 || { echo "FAIL: golden_diff compared the golden with ITSELF and returned $rc2 instead of refusing; zero differences from a self-comparison is not evidence"; exit 1; }; echo "PASS: the stored golden carries the mission-state evidence, passes golden_diff non-vacuity and quantisation guards, and self-comparison is still REFUSED (rc=2)"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$(ls goldens/windows-x64/013-constrictor/state.json tests/golden/pending/013-constrictor/state.json 2>/dev/null | head -1); P=$(ls goldens/windows-x64/013-constrictor/provenance.json tests/golden/pending/013-constrictor/provenance.json 2>/dev/null | head -1); F=$(ls goldens/windows-x64/013-constrictor/frame.grid tests/golden/pending/013-constrictor/frame.grid 2>/dev/null | head -1); "$PY" -c 'import hashlib,json,sys;g,p,f=sys.argv[1],sys.argv[2],sys.argv[3];blob=open(g,"rb").read();prov=json.load(open(p,encoding="utf-8"));arts=prov.get("artifacts") or {};rec=arts.get("state.json");sys.exit("FAIL: %s records no artifacts.state.json digest; without an INDEPENDENT witness, editing the golden changes BOTH sides of every self-comparison and no line can see it (bead oo-gxp)"%p) if not rec else None;got=hashlib.sha256(blob).hexdigest();sys.exit("FAIL: the stored golden is %d bytes but provenance records %d. The golden and the provenance that witnesses it have diverged; one of them was edited alone. A deliberate re-bless moves BOTH files together and is governed by tools/rebless-approvals.txt."%(len(blob),rec["bytes"])) if len(blob)!=rec["bytes"] else None;sys.exit("FAIL: the stored golden hashes to sha256=%s but provenance witnesses sha256=%s. ONE QUANTISED UNIT on one float is enough to do this, and a fresh-run-vs-golden or golden-vs-copy comparison CANNOT catch it because the edit moves both sides. Investigate before re-blessing; do NOT update the digest to match."%(got,rec["sha256"])) if got!=rec["sha256"] else None;frec=arts.get("frame.grid");sys.exit("FAIL: provenance records no artifacts[\"frame.grid\"] entry") if not frec else None;fb=open(f,"rb").read();sys.exit("FAIL: frame.grid is %d bytes but provenance records %d"%(len(fb),frec["bytes"])) if len(fb)!=frec["bytes"] else None;d=json.loads(blob.decode("utf-8"));ev=d.get("evidence") or {};sys.exit("FAIL: the golden carries no top-level mission_variables; the bead requires the canonical dump itself to carry the mission state") if not isinstance(d.get("mission_variables"),dict) else None;sys.exit("FAIL: the dump top-level mission_variables %r disagrees with evidence.mission_variables %r; one of the two was edited alone"%(d["mission_variables"],ev.get("mission_variables"))) if d["mission_variables"]!=ev.get("mission_variables") else None;txt=blob.decode("utf-8");sys.exit("FAIL: the dump carries a frame digest. llvmpipe is NOT bit-reproducible across runs, so a frame hash inside state.json makes the byte-identical golden comparison flake on renderer noise; frames are compared with the MEASURED tolerance and stored beside the dump as frame.grid.") if ("frame_hash" in txt or "frame_grid" in txt) else None;fc=prov.get("frame_control") or {};sys.path.insert(0,"tests/golden");import frame_hash;tol=frame_hash.derive_tolerance();sys.exit("FAIL: frame_hash.TOLERANCE has moved from the value bead oo-ae9 measured (now %r); re-measure with calibrate.py and re-bless, do NOT adjust it to make a run pass"%tol) if abs(tol-0.004377268476873758)>1e-12 else None;sys.exit("FAIL: provenance frame_control.tolerance=%r is not the measured tolerance %r"%(fc.get("tolerance"),tol)) if abs(float(fc.get("tolerance",0))-tol)>1e-12 else None;sys.exit("FAIL: provenance same_scene_distance=%r is not inside the measured tolerance %r"%(fc.get("same_scene_distance"),tol)) if not float(fc["same_scene_distance"])<tol else None;sys.exit("FAIL: provenance control_thargoidplans_distance=%r is only %.2fx the measured tolerance; the frame does not discriminate this save from another"%(fc["control_thargoidplans_distance"],float(fc["control_thargoidplans_distance"])/tol)) if float(fc["control_thargoidplans_distance"])/tol<=2 else None;print("PASS: the stored golden is byte-for-byte the artifact provenance witnesses (%d bytes, sha256=%s) and carries mission_variables at top level agreeing with evidence. NOTE THE DELIBERATE ASYMMETRY: frame.grid is recorded in provenance but NOT byte-gated - llvmpipe is not bit-reproducible, so the frame is compared with the measured tolerance %.6f, inside which two launches of this scenario sit (%.6f, %.2fx) while a DIFFERENT checklist save is outside it (%.6f, %.2fx)."%(len(blob),got,tol,fc["same_scene_distance"],float(fc["same_scene_distance"])/tol,fc["control_thargoidplans_distance"],float(fc["control_thargoidplans_distance"])/tol))' "$G" "$P" "$F"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$(ls goldens/windows-x64/013-constrictor/state.json tests/golden/pending/013-constrictor/state.json 2>/dev/null | head -1); P=$(ls goldens/windows-x64/013-constrictor/provenance.json tests/golden/pending/013-constrictor/provenance.json 2>/dev/null | head -1); "$PY" tests/golden/gate_013_differential.py "$G" "$P"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$(ls goldens/windows-x64/013-constrictor/state.json tests/golden/pending/013-constrictor/state.json 2>/dev/null | head -1); "$PY" tests/golden/gate_013_mutants.py "$G"
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; G=$(ls goldens/windows-x64/013-constrictor/state.json tests/golden/pending/013-constrictor/state.json 2>/dev/null | head -1); F=$(ls goldens/windows-x64/013-constrictor/frame.grid tests/golden/pending/013-constrictor/frame.grid 2>/dev/null | head -1); W="${LOCALAPPDATA:-/tmp}/Temp/oo_5h8e_live.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); "$PY" tests/golden/constrictor_save.py --app-dir "$APP_DIR" --out "$NW/fresh.json" --frame-out "$NW/fresh.grid" --run-root "$NW/runs" >"$W/run.log" 2>&1; rc=$?; test $rc -eq 0 || { echo "FAIL: the live Constrictor save-load run exited rc=$rc:"; cat "$W/run.log"; rm -rf "$W"; exit 1; }; echo "  live run wall=$(( $(date +%s) - t0 ))s bytes=$(wc -c < "$W/fresh.json")"; "$PY" tests/golden/check_constrictor_evidence.py "$NW/fresh.json" --label "fresh run" || { echo "FAIL: the fresh run produced a dump with no proof the Constrictor save's mission state was loaded"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$G" "$NW/fresh.json" --label-left "stored golden" --label-right "fresh run" || { echo "FAIL: a fresh run disagrees with the STORED golden. This is a FINDING, not a file to rewrite: investigate before re-blessing."; rm -rf "$W"; exit 1; }; "$PY" tests/golden/constrictor_save.py --compare-frame "$NW/fresh.grid" "$F" || { echo "FAIL: the freshly rendered frame is beyond the measured tolerance from the stored reference"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: a live run loads Constrictor.oolite-save, proves its mission state reached the running engine (4 mission variables, mission_conhunt ABSENT, all 3 oolite-constrictor-hunt handlers still live), its canonical dump is byte-identical to the stored golden, and its rendered frame matches the stored reference within the measured tolerance"
