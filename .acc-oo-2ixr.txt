bash -n tools/tier-a.sh && bash -n tools/tier-a-deny-probe.sh
bash tools/tier-a-deny-probe.sh
D="$(mktemp -d)"; trap 'rm -rf "$D"' EXIT; OOLITE_TIER_A_SOURCE_ONLY=1 . tools/tier-a.sh; set +e; R="$D/r"; mkdir -p "$R/src"; G="$(cygpath -m "$R" 2>/dev/null || printf '%s' "$R")"; git -C "$G" init -q -b main; git -C "$G" config user.email a@b; git -C "$G" config user.name a; printf 'void f(void){}\n' >"$R/src/a.c"; git -C "$G" add -A; git -C "$G" commit -qm base; git -C "$G" checkout -q -b bead; printf 'void f(void){ JS_GetContextPrivate(cx); NSAutoreleasePool *p; }\n' >"$R/src/a.c"; git -C "$G" add -A; git -C "$G" commit -qm regress; B="$(git -C "$G" merge-base HEAD main)"; deny_gate "$G" "$B" "$R/src/a.c" src/a.c >/dev/null 2>&1; dirty=$?; printf 'void f(void){}\n' >"$R/src/a.c"; deny_gate "$G" "$B" "$R/src/a.c" src/a.c >/dev/null 2>&1; clean=$?; printf 'regression_rc=%s clean_rc=%s\n' "$dirty" "$clean"; [ "$dirty" = 1 ] && [ "$clean" = 0 ]
D="$(mktemp -d)"; trap 'rm -rf "$D"' EXIT; OOLITE_TIER_A_SOURCE_ONLY=1 . tools/tier-a.sh; set +e; R="$D/r"; mkdir -p "$R/src"; G="$(cygpath -m "$R" 2>/dev/null || printf '%s' "$R")"; git -C "$G" init -q -b main; git -C "$G" config user.email a@b; git -C "$G" config user.name a; printf 'void f(void){}\n' >"$R/src/a.c"; git -C "$G" add -A; git -C "$G" commit -qm base; printf 'void f(void){ JS_GetContextPrivate(cx); }\n' >"$R/src/a.c"; git -C "$G" add -A; git -C "$G" commit -qm regress; H="$(git -C "$G" rev-parse HEAD)"; deny_gate "$G" "$H" "$R/src/a.c" src/a.c >/dev/null 2>&1; dirty=$?; printf 'void f(void){}\n' >"$R/src/a.c"; git -C "$G" add -A; git -C "$G" commit -qm cleaned; H2="$(git -C "$G" rev-parse HEAD)"; deny_gate "$G" "$H2" "$R/src/a.c" src/a.c >/dev/null 2>&1; clean=$?; printf 'head_baseline_regression_rc=%s head_baseline_clean_rc=%s\n' "$dirty" "$clean"; [ "$dirty" = 1 ] && [ "$clean" = 0 ]
D="$(mktemp -d)"; trap 'rm -rf "$D"' EXIT; OOLITE_TIER_A_SOURCE_ONLY=1 . tools/tier-a.sh; set +e; R="$D/r"; mkdir -p "$R/src"; G="$(cygpath -m "$R" 2>/dev/null || printf '%s' "$R")"; git -C "$G" init -q -b main; git -C "$G" config user.email a@b; git -C "$G" config user.name a; printf 'void f(void){ JS_GetContextPrivate(cx); }\n' >"$R/src/a.c"; git -C "$G" add -A; git -C "$G" commit -qm only; deny_gate "$G" "$(git -C "$G" rev-parse HEAD)" "$R/src/a.c" src/a.c >/dev/null 2>&1; vac=$?; out="$(OOLITE_TIER_A_BASE= OOLITE_TIER_A_BASE_BRANCH=nosuchbranch resolve_base_ref "$G" 2>"$D/e")"; rr=$?; printf 'vacuous_rc=%s resolve_rc=%s ref=[%s]\n' "$vac" "$rr" "$out"; [ "$vac" = 2 ] && [ "$rr" = 1 ] && [ -z "$out" ] && grep -q 'never fail' "$D/e"
D="$(mktemp -d)"; trap 'rm -rf "$D"' EXIT; OOLITE_TIER_A_SOURCE_ONLY=1 . tools/tier-a.sh; set +e; printf '%s\n' '\bJS_[A-Za-z_][A-Za-z0-9_]*\b' >"$D/l"; one="$(printf '%s\n' 'q = JS_A(JS_B(JS_C(x)));' | deny_count "$D/l")"; sep="$(printf '%s\n' 'a=JS_A(x);' 'b=JS_B(x);' 'c=JS_C(x);' | deny_count "$D/l")"; pack="$(printf '%s\n' 'q = JS_A(JS_B(JS_C(JS_D(x))));' | deny_count "$D/l")"; zero="$(printf 'int main(void){return 0;}\n' | deny_count "$D/l")"; printf 'one_line=%s separate=%s packed=%s zero=%s\n' "$one" "$sep" "$pack" "$zero"; [ "$one" = 3 ] && [ "$sep" = 3 ] && [ "$pack" = 4 ] && [ "$pack" -gt "$sep" ] && [ "$zero" = 0 ]
D="$(mktemp -d)"; trap 'rm -rf "$D"' EXIT; OOLITE_TIER_A_SOURCE_ONLY=1 . tools/tier-a.sh; set +e; printf '%s\n' '\bJS_[A-Za-z_][A-Za-z0-9_]*\b' 'JS_[' >"$D/bad"; printf '%s\n' '\bJS_[A-Za-z_][A-Za-z0-9_]*\b' >"$D/good"; printf 'x = JS_Foo(1);\n' | deny_count "$D/bad" >/dev/null 2>"$D/e"; badrc=$?; g="$(printf 'x = JS_Foo(1);\n' | deny_count "$D/good")"; goodrc=$?; printf 'malformed_rc=%s good_rc=%s good_count=%s\n' "$badrc" "$goodrc" "$g"; [ "$badrc" = 2 ] && grep -q 'unusable' "$D/e" && [ "$goodrc" = 0 ] && [ "$g" = 1 ]
! grep -vE '^[[:space:]]*#' tools/tier-a.sh | grep -nE 'merge-base[^|]*\|\|[[:space:]]*echo HEAD'
! grep -vE '^[[:space:]]*#' tools/tier-a.sh | grep -n 'grep -cE'
grep -vE '^[[:space:]]*#' tools/tier-a.sh | grep -n 'grep -oE'
