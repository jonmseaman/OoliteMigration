PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); for f in tests/golden/combat.py tests/golden/check_combat_evidence.py tests/golden/test_combat.py tests/golden/gate_003_spec.py tests/golden/pending/003-combat/spec.json tests/golden/pending/003-combat/README.md tests/golden/pending/003-combat/LANDING.md; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; G=$(ls goldens/windows-x64/003-combat/state.json tests/golden/pending/003-combat/state.json 2>/dev/null | head -1); P=$(ls goldens/windows-x64/003-combat/provenance.json tests/golden/pending/003-combat/provenance.json 2>/dev/null | head -1); for f in "$G" "$P"; do test -n "$f" && test -f "$f" || { echo "MISSING: scenario 003 golden or provenance (guarded goldens/ searched first, then tests/golden/pending/)"; exit 1; }; done; "$PY" -m py_compile tests/golden/combat.py tests/golden/check_combat_evidence.py tests/golden/test_combat.py tests/golden/gate_003_spec.py && echo "PASS: scenario 003-combat script, spec, staged golden, provenance and evidence checker are present and importable (golden=$G)"
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" tests/golden/gate_003_spec.py || { echo "FAIL: scenario 003 determinism knobs are not pinned, not read by combat.py, or disagree with the knobs the staged golden was blessed with"; exit 1; }
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); "$PY" -m pytest tests/golden/test_combat.py -q -p no:cacheprovider
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$(ls goldens/windows-x64/003-combat/state.json tests/golden/pending/003-combat/state.json 2>/dev/null | head -1); "$PY" tests/golden/check_combat_evidence.py "$G" --label "staged golden 003-combat" || { echo "FAIL: the stored golden does not prove a combat encounter happened; a byte-identical comparison against it would be vacuous"; exit 1; }
PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); G=$(ls goldens/windows-x64/003-combat/state.json tests/golden/pending/003-combat/state.json 2>/dev/null | head -1); W="${LOCALAPPDATA:-/tmp}/Temp/oo_izi_gate.$$"; rm -rf "$W"; mkdir -p "$W"; cp "$G" "$W/copy.json"; cp "$(dirname "$G")/provenance.json" "$W/provenance.json"; C=$(cygpath -m "$W/copy.json" 2>/dev/null || echo "$W/copy.json"); "$PY" tests/golden/golden_diff.py "$G" "$C" --label-left "staged golden" --label-right "an untouched copy"; rc=$?; test $rc -eq 0 || { echo "FAIL: golden_diff refuses or rejects the staged golden (rc=$rc); it is off-policy, collapsed, or all-whole-number"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$G" "$G" >/dev/null 2>&1; rc2=$?; rm -rf "$W"; test $rc2 -eq 2 || { echo "FAIL: golden_diff compared the golden with ITSELF and returned $rc2 instead of refusing; zero differences from a self-comparison is not evidence"; exit 1; }; echo "PASS: the staged golden passes golden_diff non-vacuity and quantisation guards, and self-comparison is still REFUSED (rc=2)"
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; G=$(ls goldens/windows-x64/003-combat/state.json tests/golden/pending/003-combat/state.json 2>/dev/null | head -1); W="${LOCALAPPDATA:-/tmp}/Temp/oo_izi_two.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); for i in 1 2; do t0=$(date +%s); "$PY" tests/golden/combat.py --app-dir "$APP_DIR" --out "$NW/run$i.json" --run-root "$NW/runs" >"$W/run$i.log" 2>&1 || { echo "FAIL: run $i of the two-run combat proof exited nonzero:"; cat "$W/run$i.log"; rm -rf "$W"; exit 1; }; echo "  run$i wall=$(( $(date +%s) - t0 ))s bytes=$(wc -c < "$W/run$i.json")"; done; for i in 1 2; do "$PY" tests/golden/check_combat_evidence.py "$NW/run$i.json" --label "fresh run $i" || { echo "FAIL: fresh run $i produced a dump with no engine evidence that combat occurred"; rm -rf "$W"; exit 1; }; done; "$PY" tests/golden/golden_diff.py "$NW/run1.json" "$NW/run2.json" --label-left "fresh run 1" --label-right "fresh run 2" || { echo "FAIL: two independent combat runs differ"; rm -rf "$W"; exit 1; }; "$PY" tests/golden/golden_diff.py "$G" "$NW/run1.json" --label-left "staged golden" --label-right "fresh run 1" || { echo "FAIL: a fresh run disagrees with the STAGED golden. This is a FINDING, not a file to rewrite: investigate before re-blessing."; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: two independent combat runs each carry engine damage/death evidence, agree with each other, and agree with the staged golden"
export PATH=/ucrt64/bin:$PATH; PY=$(command -v python3 || command -v python || echo /ucrt64/bin/python3); APP_DIR="${OO_APP_DIR:-C:/Users/jon/OoliteMigration/upstream/oolite/build/meson_test/oolite.app}"; test -d "$APP_DIR" || { echo "no Oolite build at $APP_DIR; build it first (tools/build-windows.sh test)"; exit 1; }; W="${LOCALAPPDATA:-/tmp}/Temp/oo_izi_mut.$$"; rm -rf "$W"; mkdir -p "$W"; NW=$(cygpath -m "$W" 2>/dev/null || echo "$W"); t0=$(date +%s); "$PY" tests/golden/combat.py --app-dir "$APP_DIR" --no-fire --out "$NW/dead.json" --run-root "$NW/runs" >"$W/dead.log" 2>&1; rc=$?; w=$(( $(date +%s) - t0 )); test $rc -ne 0 || { echo "FAIL: a run in which the attacker never fired still exited 0 and produced a dump. The scenario would bless a golden that proves nothing about combat."; cat "$W/dead.log"; rm -rf "$W"; exit 1; }; grep -q "evidence.damage_events" "$W/dead.log" || { echo "FAIL: the no-fire run failed for some reason OTHER than the missing combat evidence (rc=$rc):"; cat "$W/dead.log"; rm -rf "$W"; exit 1; }; test ! -f "$W/dead.json" || { echo "FAIL: the no-fire run wrote a dump despite failing its evidence assertions"; rm -rf "$W"; exit 1; }; rm -rf "$W"; echo "PASS: with the attacker prevented from firing the scenario REFUSES to dump (rc=$rc, wall=${w}s), naming evidence.damage_events - the gate can go red"
bash tools/guardrails.sh && test -z "$(git status --porcelain)"